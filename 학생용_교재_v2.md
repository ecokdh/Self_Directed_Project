# 시계열 예측·이상진단 현장실습 교재 (학생용)

**아주대학교 현장실습 — 시계열 예측 트랙**
**지도 주체: (주)블루웍스** | 12주 · 주 8시간 · 총 96시간

---

## 목차

- [0장. 시작하기](#0장-시작하기)
- **1단계 — 노트북(CPU) 6주**
  - [1주차. 데이터셋 고찰 ① — BDG2 해부](#1주차-데이터셋-고찰--bdg2-해부)
  - [2주차. 데이터셋 고찰 ② — 평가 하네스 구축](#2주차-데이터셋-고찰--평가-하네스-구축)
  - [3주차. LightGBM 예측](#3주차-lightgbm-예측)
  - [4주차. LightGBM 이상진단 + SHAP 설명](#4주차-lightgbm-이상진단--shap-설명)
  - [5주차. TSFM 입문 — Chronos-2 zero-shot](#5주차-tsfm-입문--chronos-2-zero-shot)
  - [6주차. 1단계 통합 및 중간 발표](#6주차-1단계-통합-및-중간-발표)
- **2단계 — 서버(GPU) 6주**
  - [7주차. TSFM 심화 — LoRA 파인튜닝과 오염 검증](#7주차-tsfm-심화--lora-파인튜닝과-오염-검증)
  - [8주차. 예측 전용 딥러닝](#8주차-예측-전용-딥러닝)
  - [9주차. 이상진단 전용 모델](#9주차-이상진단-전용-모델)
  - [10주차. 멀티에이전트 ① — 모델 라우팅](#10주차-멀티에이전트--모델-라우팅)
  - [11주차. 멀티에이전트 ② — 오차 보정과 근거 설명 생성](#11주차-멀티에이전트--오차-보정과-근거-설명-생성)
  - [12주차. 최종 통합 및 발표](#12주차-최종-통합-및-발표)
- [부록 A. 주간 보고서 양식](#부록-a-주간-보고서-양식)
- [부록 B. 평가지표 치트시트](#부록-b-평가지표-치트시트)
- [부록 C. 트러블슈팅](#부록-c-트러블슈팅)

---

# 0장. 시작하기

## 0.1 이 과정에서 무엇을 하는가

여러분은 12주 동안 **건물 에너지 수요 예측**이라는 하나의 문제를 붙들고, 네 계열의 접근법을 직접 겨루게 합니다.

| 계열                         | 언제 배우나 | 대표                                     |
| ---------------------------- | ----------- | ---------------------------------------- |
| 부스팅 트리                  | 3~4주차     | LightGBM                                 |
| 시계열 파운데이션 모델(TSFM) | 5·7주차    | Chronos-2, TTM                           |
| 예측 전용 딥러닝             | 8주차       | TimeXer, iTransformer, PatchTST, DLinear |
| 이상진단 전용 모델           | 9주차       | CATCH, DADA, Sub-LOF, Matrix Profile     |

그리고 10~12주차에 이들을 **상황에 따라 골라 쓰는 에이전트 시스템**으로 묶고, 예측 근거를 설명하는 리포트를 자동 생성합니다.

### 이 과정이 요구하지 않는 것

**특정 모델이 이기는 것을 요구하지 않습니다.**

최신 파운데이션 모델이 LightGBM에 질 수도 있습니다. ICLR 논문의 트랜스포머가 선형 한 층짜리 DLinear에 질 수도 있습니다. 여러분이 만든 라우팅 에이전트가 단일 모델보다 못할 수도 있습니다. **이 결과들은 전부 정상이고, 현장에서 실제로 매일 일어나는 일입니다.**

요구하는 것은 세 가지입니다.

1. **판정 가능성** — 어떤 결과가 나오든 정량적으로 판정하고 원인을 설명한다.
2. **재현 가능성** — 2주차에 만든 하네스로 12주차까지 모든 결과가 같은 조건에서 비교된다.
3. **함정 인식** — 데이터 누수, 사전학습 오염, look-back 민감도, point-adjust 부풀림. 이 네 가지를 직접 재현해 보고 회피 방법을 안다.

3번이 이 과정의 존재 이유입니다. 대부분의 교육은 모델 사용법을 가르치고 끝나지만, **기업 현장에서 프로젝트가 실패하는 원인은 모델이 아니라 잘못된 평가에서 나온 잘못된 확신**인 경우가 압도적으로 많습니다.

## 0.2 선수지식

| 항목           | 요구 수준                            |
| -------------- | ------------------------------------ |
| Python         | 함수·클래스 작성, 가상환경 사용     |
| pandas / NumPy | DataFrame 조작, 시간 인덱스 리샘플링 |
| scikit-learn   | 학습·예측·교차검증 API             |
| 머신러닝 기초  | 과적합, 학습/검증 분리, 지표의 의미  |
| Git            | commit / branch                      |

**본 과정은 위 항목에 수업 시간을 배정하지 않습니다.** 부족한 부분은 1주차 시작 전에 스스로 보완하십시오.

## 0.3 환경 구축

### 1단계 (1~6주차) — 자기 노트북

| 항목       | 요구 사양                             |
| ---------- | ------------------------------------- |
| RAM        | 8GB 이상 (16GB 권장)                  |
| 저장공간   | 10GB 이상 여유                        |
| GPU        | **불필요**                      |
| OS         | Windows / macOS / Linux               |
| **Python** | **3.11 권장** (3.12·3.13 설치 가능 / **3.14 불가** — 6주차 `granite-tsfm`이 3.14를 지원하지 않습니다) |

1단계 전 과제는 CPU만으로 수 분 안에 끝납니다. 실측 기준으로 가장 무거운 작업(Chronos-2 zero-shot)이 건물 1개당 0.07초, 가장 큰 메모리 점유가 약 1GB입니다. **연산 자원 때문에 과제를 줄일 필요는 없습니다.**

```bash
# 1단계 환경
python -m venv .venv
# Windows:  .venv\Scripts\activate
# macOS/Linux:  source .venv/bin/activate

pip install -r requirements_stage1.txt
```

**`requirements_stage1.txt`는 본 교재와 같은 폴더에 있습니다.** 그 파일을 그대로 설치하십시오. **임의로 버전을 올리지 마십시오.** 12주 누적 비교표는 1주차 결과와 12주차 결과를 같은 표에 올리는 문서입니다. 중간에 버전이 바뀌면 그 표가 성립하지 않습니다.

포함 패키지: `pandas`, `numpy`, `pyarrow`, `scikit-learn`, `lightgbm`, `shap`, `matplotlib`, `jupyterlab`, `torch`(CPU 빌드), `transformers`, **`chronos-forecasting`**(5주차), **`granite-tsfm`**(6주차), `psutil`

> **`transformers`는 `4.57.6`으로 상한이 걸려 있습니다.** `granite-tsfm`은 `<6`까지만 선언해 `transformers` 5.x가 그대로 허용됩니다. 핀 없이 설치하면 최신 `transformers`가 딸려 들어오고 조합에 따라 6주차 TTM이 로드 단계에서 죽습니다(`AttributeError: ... 'all_tied_weights_keys'`). **이름만 보고 `pip install transformers`로 따로 깔지 마십시오.**

**OS별 주의 — `torch` CPU 빌드**

| OS                | 설치 방법                                                                  |
| ----------------- | -------------------------------------------------------------------------- |
| Windows           | 그냥 설치하면 CPU 빌드입니다. 추가 조치 불필요                             |
| Linux             | 기본 휠이 CUDA 번들(수 GB)입니다.`--index-url https://download.pytorch.org/whl/cpu` 를 반드시 사용 |
| macOS             | 기본이 CPU/MPS 빌드입니다. 추가 조치 불필요                                |

설치 후 다음으로 확인하십시오. `None`이 나와야 CPU 빌드입니다.

```python
import torch; print(torch.__version__, torch.version.cuda)   # 예: 2.10.0+cpu None
```

### Windows 사용자 필독 — 두 가지 함정

**① 파일 인코딩 (한글 Windows)**

한글 Windows의 Python 기본 인코딩은 UTF-8이 아니라 **cp949**입니다. 그래서 이런 일이 생깁니다.

```python
open("근거.md", "w").write("선정 근거: 결측률 2% 미만")   # cp949로 저장됨
open("근거.md").read()                                    # 다른 환경에서 열면 UnicodeDecodeError
pd.read_csv("결과.csv")                                   # pandas는 UTF-8 기본이라 이건 됨 (그래서 더 헷갈림)
```

**규칙: 모든 파일 입출력에 `encoding="utf-8"`을 명시합니다.** 0.5절 작업 규칙에 포함되어 있습니다. 아울러 환경변수 `PYTHONUTF8=1` 설정을 권장합니다.

**② HuggingFace 캐시**

5·6주차에 모델 가중치를 내려받습니다. Windows는 기본적으로 symlink를 지원하지 않아 다음 경고와 함께 캐시가 중복 저장됩니다.

```
UserWarning: `huggingface_hub` cache-system uses symlinks ... your machine does not support them
```

동작 자체에는 문제가 없습니다. 디스크가 빠듯하면 Windows **개발자 모드**를 켜거나, 캐시 위치를 여유 있는 드라이브로 옮기십시오.

```bash
# Windows (PowerShell)
setx HF_HOME "D:\hf_cache"
```

### 2단계 (7~12주차) — 사내 GPU 서버

접속 정보와 계정은 6주차에 별도 안내합니다. 서버에는 `requirements_stage2.txt`가 별도로 동결되어 있습니다.

**서버 사용 규칙**

- GPU를 다른 작업과 공유할 수 있습니다. 장시간 학습 전에 `nvidia-smi`로 점유 상태를 확인하십시오.
- 작업 디렉터리는 `/home/{학번}/` 아래에만 만듭니다.
- 학습 로그와 체크포인트는 반드시 자기 디렉터리에 저장합니다.

## 0.4 데이터 준비

| 데이터                                          | 용도                 | 사용 주차       | 기간        |
| ----------------------------------------------- | -------------------- | --------------- | ----------- |
| **BDG2** (Building Data Genome Project 2) | 예측 주축 데이터     | 1~8, 10~12주차 | 2016~2017  |
| **LEAD 1.0**                              | 이상진단 라벨 데이터 | 4·9주차        | **2016만** |

**본 과정이 쓰는 판본은 아래로 고정되어 있습니다.** 다른 판본을 받으면 교재의 후보 목록·조인 결과와 어긋납니다.

| 데이터 | 입수 경로 | 받을 파일 | 규모 |
| --- | --- | --- | --- |
| **BDG2** | GitHub `buds-lab/building-data-genome-project-2` | `data/metadata/metadata.csv`, 미터별 CSV(`electricity.csv` 등), `weather.csv` | 건물 1,636개 · 2016~2017 |
| **LEAD 1.0** | GitHub `samy101/lead-dataset` → **`data/lead1.0-small.zip`** (공개본) | 압축 해제 후 `lead1.0-small.csv` | 약 10MB · **건물 200개** · **2016년만** · 이상률 2.13% |

> **⚠️ LEAD는 판본을 반드시 확인하십시오.** Kaggle 대회 전체본(1,413미터)이 따로 돌아다니지만 **본 과정은 공개본 200개를 표준으로 씁니다.** 4주차 조인 결과와 후보 목록이 전부 이 판본 기준입니다.

> **⚠️ BDG2는 Git LFS 저장소입니다.** `raw.githubusercontent.com` 링크로 받으면 실제 데이터가 아니라 **약 130바이트짜리 LFS 포인터 텍스트**가 받아지고, `pd.read_csv`가 오류 없이 3행짜리 이상한 표를 반환합니다. `git lfs pull`을 쓰거나, 단일 파일은 **`media.githubusercontent.com/media/...`** 경로로 받으십시오. **받은 뒤 파일 크기를 확인하는 것이 가장 확실합니다.**

지도자가 사내 스토리지에 사본을 준비해 둔 경우에는 그 사본을 쓰십시오. 없으면 위 경로에서 직접 받되, **받은 날짜와 파일 크기를 기록해 두십시오** — 12주차에 README의 재현 절차를 쓸 때 필요하고, 중간에 데이터가 바뀌지 않았음을 증명하는 유일한 근거입니다.

BDG2 주요 파일:

| 파일                               | 내용                                            |
| ---------------------------------- | ----------------------------------------------- |
| `metadata.csv`                   | 건물 메타데이터 (site, 용도, 면적, 준공년도 등) |
| `electricity.csv` 등 미터별 파일 | 시간 단위 계측값. 열 = 건물, 행 = 시각          |
| `weather.csv`                    | site별 기상 (외기온, 이슬점온도, 풍속 등)       |

### 두 데이터를 잇는 키 — 미리 알아둘 것

BDG2와 LEAD는 **건물 식별자 체계가 다릅니다.**

| 데이터 | 식별자         | 예                       |
| ------ | -------------- | ------------------------ |
| BDG2   | 명명형         | `Panther_office_Shauna` |
| LEAD   | 익명 숫자      | `1`, `32`, `41`     |

둘을 잇는 열은 **BDG2 `metadata.csv`의 `building_id_kaggle`** 입니다. 이 열이 LEAD의 `building_id`와 1:1로 대응합니다. 4주차에 쓰게 됩니다.

> **⚠️ 조인 시 dtype 함정** — `building_id_kaggle`은 결측이 있어 **float**(`0.0`, `NaN`)이고 LEAD의 `building_id`는 **int64**입니다. 그대로 `merge`하면 **오류 없이 매칭 0건**이 나옵니다. 조인 전에 형을 맞추고, **조인 직후 매칭된 건물 수를 반드시 출력해 확인**하십시오.

### 대상 건물 후보 목록

**LEAD는 BDG2의 모든 건물에 라벨을 붙이지 않았습니다.** 공개본 200개는 BDG2 전기미터 보유 건물 1,578개의 **12.7%**뿐입니다. 그래서 **라벨 보유 건물 후보 목록**(`lead_labeled_candidates.csv`, 200행)이 **본 교재와 같은 폴더에 들어 있습니다.**

**1주차 서브셋은 반드시 이 목록 안에서 고릅니다.** 목록 밖에서 고르면 4주차에 이상진단 과제를 수행할 수 없고, 서브셋은 12주차까지 바꿀 수 없으므로 되돌릴 방법이 없습니다.

## 0.5 작업 규칙

| 항목                | 규칙                                                                  |
| ------------------- | --------------------------------------------------------------------- |
| 코드 저장소         | 각자 개인 Git 저장소를 만들고 매주 commit                             |
| 노트북 파일명       | `wk{주차2자리}_{주제}.ipynb` (예: `wk03_lightgbm_forecast.ipynb`) |
| 결과 저장           | 모든 실험 결과는`results/` 아래 CSV로 저장. 그래프만 남기지 말 것   |
| **파일 인코딩** | **모든 파일 입출력에 `encoding="utf-8"` 명시.** 예외 없음        |
| 측정값 기록         | 시간·메모리를 측정할 때는**측정 조건(스레드 수, 배치 크기, 반복 횟수)을 함께 기록** |
| **기준 대비 기록** | 모든 결과를**2주차 Seasonal Naive 대비 증감(%)**과 함께 기록. 절대값만 적지 말 것 |
| 보고서              | 매주 금요일 18시, 부록 A 양식                                         |

**"파일 인코딩" 규칙의 이유**: 결과 파일은 지도자가 열고, 12주차에 여러분 자신이 다시 엽니다. 한글 Windows에서 인코딩을 명시하지 않으면 cp949로 저장되어 다른 환경에서 깨집니다. `open()`, `to_csv()`, `json.dump()` 전부 해당합니다.

**"측정값 기록" 규칙의 이유**: "Chronos-2가 LightGBM보다 몇 배 느린가"의 답은 **비교 방식에 따라 3배도 되고 70배도 됩니다.** 조건을 안 적으면 **5주차에 적은 숫자와 6주차에 적은 숫자조차** 비교할 수 없습니다. 과제 5-6에서 이 문제를 직접 겪게 됩니다.

**"기준 대비 기록" 규칙의 이유**: 여러분은 이 과정을 혼자 수행합니다. **결과를 대조할 상대가 없으므로, 유일한 비교 기준은 2주차에 확정한 자기 베이스라인입니다.** "MASE 0.71"은 혼자 보면 아무 의미가 없고, "0.82 → 0.71, 13% 개선"이라야 판정이 됩니다. 주간 보고서 4번이 매주 이것을 요구하는 이유이기도 합니다.

---

# 1단계 — 노트북(CPU) 6주

> **1단계의 목표는 "기준선을 세우는 것"입니다.** 최신 모델을 쓰기 전에, 무엇과 비교할지를 먼저 확정합니다.

---

# 1주차. 데이터셋 고찰 ① — BDG2 해부

## 학습목표

1. BDG2의 구조를 파악하고 분석 대상 건물 서브셋을 근거를 갖고 선정한다.
2. 결측 패턴을 분류하고, 각 패턴에 맞는 처리 방식을 선택한다.
3. **이상치(outlier)와 진짜 이상(anomaly)의 차이**를 데이터로 설명한다.
4. 데이터 누수(leakage)가 무엇이고 시계열에서 왜 특히 위험한지 설명한다.

## 배경 개념

### 왜 모델보다 데이터를 먼저 보는가

여러분은 이미 여러 모델을 알고 있을 것입니다. 그러나 실무에서 예측 프로젝트가 실패할 때, 원인이 모델 선택인 경우는 드뭅니다. 대부분은 다음 셋 중 하나입니다.

- 데이터에 미래 정보가 섞여 들어갔다 (누수)
- 결측을 잘못 메워서 없던 패턴을 만들었다
- 평가 구간이 우연히 쉬운 기간이었다

1~2주차는 이 셋을 전부 차단하는 데 씁니다.

### 결측의 세 가지 유형

| 유형                  | 모습                              | 처리 방향                                 |
| --------------------- | --------------------------------- | ----------------------------------------- |
| **산발적 결측** | 한두 시점이 비어 있음             | 선형 보간 또는 직전값                     |
| **연속 결측**   | 며칠~몇 주가 통째로 비어 있음     | 보간하지 말 것. 해당 구간을 평가에서 제외 |
| **구조적 결측** | 특정 건물의 특정 미터가 아예 없음 | 해당 건물·미터를 대상에서 제외           |

**가장 흔한 실수**: 연속 결측을 선형 보간으로 메우면 완벽한 직선이 만들어집니다. 모델은 그 직선을 학습하고, 평가에서도 그 직선을 맞히면서 실제보다 좋은 점수를 냅니다.

### 데이터 누수 (Leakage)

**미래 시점의 정보가 과거 시점의 입력에 섞여 들어가는 것**입니다. 시계열에서 특히 위험한 이유는, 일반적인 전처리 습관이 그대로 누수가 되기 때문입니다.

| 누수를 만드는 흔한 습관               | 왜 누수인가                       |
| ------------------------------------- | --------------------------------- |
| 전체 데이터로`StandardScaler.fit()` | 평균·표준편차에 미래 값이 반영됨 |
| 결측을 전체 평균으로 대체             | 그 평균에 미래 값이 들어 있음     |
| 전체 기간 기준으로 이상치 제거        | 미래를 알아야 판단 가능한 제거    |
| 무작위 셔플 후 train/test 분할        | 미래 시점이 학습에 들어감         |

**원칙**: 어떤 전처리든 **학습 구간의 통계만으로** 정의하고, 그것을 검증·평가 구간에 적용합니다.

### 이상치 vs 이상

|      | 이상치(outlier)             | 이상(anomaly)                           |
| ---- | --------------------------- | --------------------------------------- |
| 정의 | 통계적으로 드문 값          | **설비·운영상 문제가 있는 상태** |
| 예   | 한여름 최고 부하일의 피크   | 냉동기 고장으로 인한 소비 급증          |
| 처리 | 정상 데이터. 제거하면 안 됨 | 탐지 대상                               |

여름 피크를 "이상치"라며 제거하면, 정작 예측해야 할 가장 중요한 순간을 못 맞히는 모델이 됩니다.

## 실습 과제

**과제 1-1. 데이터 로드와 구조 파악**

- `metadata.csv`를 읽고 site별·용도별 건물 수를 집계한다.
- `electricity.csv`의 shape, 기간, 시간 간격을 확인한다.

**과제 1-2. 분석 대상 서브셋 선정**

- **모집단은 교재와 같은 폴더의 `lead_labeled_candidates.csv` (200행) 입니다.** 이 목록 안에서만 고릅니다 (0.4절 참조)
- 다음 조건으로 **10~20개 건물**을 선정한다.
  - **후보 목록에 포함될 것** (필수. 이유는 4주차에 드러납니다)
  - 전체 기간 결측률이 낮은 순
  - 최소 2개 이상의 서로 다른 용도(primary space usage)를 포함할 것
- **선정 기준과 그 이유를 문서화한다.** 이 서브셋을 12주차까지 바꾸지 않습니다.

> **왜 모집단이 제한되어 있는가** — 지금은 "그렇게 하라"로 받아들이십시오. 4주차에 그 이유를 직접 확인하게 됩니다. **되돌릴 수 없는 결정이 프로젝트 초반에 몰려 있다**는 것이 이 과정에서 배울 것 중 하나입니다.

**과제 1-3. 결측 프로파일링**

- 선정한 건물별로 결측률, 최장 연속 결측 길이를 계산한다.
- 결측을 위 세 유형으로 분류하고 건물별 분포를 표로 만든다.

**과제 1-4. 결측 처리 3안 비교**

- (A) 선형 보간 (B) 직전값 채우기 (C) 연속 결측은 마스킹 후 평가 제외
- 세 방식을 적용한 결과를 시각화하고, **하나를 선택해 그 이유를 쓴다.**

**과제 1-5. 기본 EDA**

- 일간·주간 주기성 확인 (시간대별 평균, 요일별 평균)
- 외기온과 전력 소비의 산점도. 냉방·난방 구간이 보이는가?

## 산출 기준

- [ ] 대상 건물 10~20개 선정 완료, **선정 기준 문서화**
- [ ] 건물별 결측 프로파일 표
- [ ] 결측 처리 3안 비교 그래프 + 선택 근거 1문단
- [ ] 시간대별·요일별 평균 그래프
- [ ] 외기온 vs 소비 산점도
- [ ] 주간 보고서 (부록 A)

## 내 결과가 정상인가 — 판정 기준

> 아래 표는 **자기 결과를 스스로 판정하기 위한 기준**입니다. 절대값은 여러분이 고른 서브셋에 따라 달라지므로, 숫자보다 **패턴이 맞는지**를 보십시오.

| 관찰                                          | 판정                                                            |
| --------------------------------------------- | --------------------------------------------------------------- |
| 시간대별 평균에 뚜렷한 이중 피크 또는 근무시간 패턴 | 정상                                                            |
| 요일별 평균에서 주말이 낮음                   | 정상 (사무·교육 용도)                                          |
| 외기온-소비 산점도가 V자 또는 하키스틱        | 정상 (냉방·난방 전환점)                                        |
| **산점도가 완전 무상관**                | 건물 용도를 확인하십시오. 데이터센터·공장이면 정상, 사무동이면 **데이터 오류 의심** |

## 자가 점검 질문

1. 내가 고른 결측 처리 방식은 누수를 만드는가? 만들지 않는다면 왜인가?
2. 내가 선정한 건물들은 특별히 예측하기 쉬운 건물만 모은 것은 아닌가?
3. 외기온-소비 산점도에서 냉방 구간과 난방 구간의 기울기가 다른가? 다르다면 그 의미는?
4. "이 건물의 3월 15일 값은 이상치인가 이상인가"를 지금 판단할 수 있는가? 판단하려면 무엇이 더 필요한가?

## 참고자료

- BDG2 데이터 논문 및 저장소 (`buds-lab/building-data-genome-project-2`)
- 시계열 누수에 관한 scikit-learn `TimeSeriesSplit` 문서

---

# 2주차. 데이터셋 고찰 ② — 평가 하네스 구축

## 학습목표

1. 롤링 오리진 백테스트를 직접 구현한다.
2. MAE·RMSE·MASE·wQL의 차이를 설명하고 상황에 맞게 선택한다.
3. Seasonal Naive 베이스라인 점수를 확정한다.

> **이 주가 12주 전체에서 가장 중요합니다.** 여기서 만든 하네스를 12주차까지 그대로 씁니다. 이후 모든 모델은 이 하네스에 끼워 넣기만 하면 됩니다. **하네스를 나중에 바꾸면 그 전 결과와 비교가 불가능해집니다.**

## 배경 개념

### 롤링 오리진 백테스트

단일 홀드아웃(마지막 한 달을 테스트로 쓰기)은 위험합니다. 그 한 달이 유난히 쉽거나 어려운 기간일 수 있기 때문입니다.

롤링 오리진은 예측 시작점(origin)을 여러 개 잡습니다.

```
전체 기간: |------------------------------------------|

윈도우 1:  |---- train ----|-- test --|
윈도우 2:      |---- train ----|-- test --|
윈도우 3:          |---- train ----|-- test --|
                              ...
```

**설계 결정 3가지 — 직접 정하고 문서화할 것**

| 결정               | 선택지                           | 고려사항                                              |
| ------------------ | -------------------------------- | ----------------------------------------------------- |
| 학습창 방식        | 확장(expanding) vs 고정(sliding) | 확장은 데이터를 다 쓰고, 고정은 오래된 패턴을 버림    |
| 예측 지평(horizon) | 24h / 168h(1주)                  | 현장 용도에 따라. 본 과정은**24h와 168h 둘 다** |
| 윈도우 개수        | 최소 8개 이상                    | 적으면 운에 좌우됨                                    |

### 지표 선택

| 지표           | 정의                          | 특성                                               | 쓸 때                            |
| -------------- | ----------------------------- | -------------------------------------------------- | -------------------------------- |
| **MAE**  | 절대오차 평균                 | 큰 오차에 관대                                     | 오차 크기가 선형적으로 중요할 때 |
| **RMSE** | 제곱오차 평균의 제곱근        | **큰 오차에 민감**                           | 피크 오차가 치명적일 때          |
| **MASE** | MAE ÷ (**학습 구간** Seasonal Naive의 MAE) | **스케일 무관**. 1보다 작으면 Naive보다 나음 | 여러 건물을 평균낼 때            |
| **wQL**  | 분위수 손실의 가중 평균       | **확률예측 평가**                            | 예측구간이 필요할 때             |

**결정적으로 중요한 점**: 건물마다 소비 규모가 수십 배 차이 납니다. 정규화 없이 RMSE를 평균내면 **가장 큰 건물 하나가 전체 점수를 지배**합니다. 여러 건물을 묶어 하나의 숫자로 만들 때는 **MASE를 쓰거나, 건물별로 정규화한 뒤 평균**하십시오.

### Seasonal Naive

"1주일 전 같은 시각의 값"을 그대로 예측값으로 씁니다.

```
ŷ(t) = y(t - 168)     # 시간 단위 데이터, 주간 주기
```

이 단순한 규칙이 놀랍도록 강합니다. **여러분이 만들 모든 모델은 이것과 비교됩니다.** 12주 내내 개선율의 분모가 이 점수입니다.

> **⚠️ MASE의 분모는 반드시 「학습 구간」의 Seasonal Naive MAE로 계산하십시오.**
>
> 테스트 구간의 Naive MAE로 나누면 **그 자체가 누수**입니다. 분모에 평가 대상 기간의 실제값이 들어가기 때문입니다. 코드가 오류를 내지 않고 숫자도 그럴듯하게 나오므로 **끝까지 발견되지 않습니다.**
>
> ```python
> mae_naive = mean_abs_error(train_y[168:], train_y[:-168])   # ⭕ 학습 구간에서
> mase      = mae_model / mae_naive
> ```
>
> **이것은 2주차에서 가장 중요한 한 줄입니다.** 여기가 틀리면 12주 내내 쓰는 모든 개선율이 오염됩니다. 과제 2-3을 끝낸 뒤 **Seasonal Naive 자신의 MASE를 계산해 보십시오. 1.0 근처가 나와야 정상이고, 그것이 분모 정의가 맞았다는 검산입니다.**

## 실습 과제

**과제 2-1. 백테스트 하네스 구현**

다음 인터페이스를 만족하는 재사용 가능한 모듈을 작성한다.

```python
def rolling_backtest(
    data,              # 시계열 DataFrame
    model_fn,          # (train_df) -> 학습된 예측기
    predict_fn,        # (예측기, horizon) -> 예측값
    n_windows=8,
    horizon=24,
    train_mode="expanding",   # or "sliding"
):
    """윈도우별·건물별 예측 결과를 tidy 형태로 반환"""
    return results_df   # columns: window, building, timestamp, y_true, y_pred
```

**반환 형태를 tidy DataFrame으로 고정하는 것이 핵심입니다.** 이후 모든 모델이 같은 형태로 결과를 내면 지표 계산 코드를 한 번만 쓰면 됩니다.

**과제 2-2. 지표 모듈 구현**

- `mae`, `rmse`, `mase`, `wql`을 구현한다.
- 입력은 과제 2-1의 tidy DataFrame, 출력은 건물별 점수 + 전체 집계 점수.
- **집계 방식(단순평균 / 가중평균 / 정규화 후 평균)을 선택하고 이유를 문서화한다.**

**과제 2-3. Seasonal Naive 베이스라인 확정**

- 하네스에 Seasonal Naive를 끼워 넣고 실행한다.
- horizon 24h와 168h 각각에 대해 점수를 낸다.
- **결과를 `results/baseline_seasonal_naive.csv`로 저장한다.** 이 파일은 12주차까지 유지합니다.

**과제 2-4. 누수 검증**

- 일부러 누수를 만든 버전(전체 데이터로 스케일링)과 정상 버전의 점수를 비교한다.
- **차이가 얼마나 나는지 수치로 기록한다.** 누수가 얼마나 달콤한 거짓말인지 체감하는 것이 목적입니다.

## 산출 기준

- [ ] `rolling_backtest` 하네스 (재사용 가능, 문서화된 인터페이스)
- [ ] 지표 모듈 4종 (MAE·RMSE·MASE·wQL)
- [ ] 백테스트 설계 결정 3가지의 선택과 근거
- [ ] **Seasonal Naive 점수 확정** — horizon 24h·168h, CSV 저장
- [ ] 누수 유무에 따른 점수 차이 실험 결과
- [ ] 주간 보고서

## 내 결과가 정상인가 — 판정 기준

| 관찰                                          | 판정                                                            |
| --------------------------------------------- | --------------------------------------------------------------- |
| **Seasonal Naive 자신의 MASE = 1.0**    | **정상.** 1.0이 아니면 분모 정의가 틀렸습니다 — 학습 구간으로 계산했는지 확인 |
| 윈도우별 점수의 표준편차가 평균의 10~30%     | 정상                                                            |
| **누수 버전이 정상 버전보다 20% 이상 좋음** | **정상.** 그것이 과제 2-4의 요점입니다                    |
| **누수 버전과 정상 버전 점수가 거의 같음** | **누수를 제대로 만들지 못한 것입니다.** 스케일러가 정말 전체 구간으로 fit 되었는지 확인하고 다시 하십시오 |

## 자가 점검 질문

1. 내 하네스는 학습 구간의 통계만으로 전처리하는가? 검증할 수 있는가?
2. 윈도우 8개의 점수 분산은 얼마인가? 분산이 크다면 무슨 의미인가?
3. 누수 버전의 점수는 정상 버전보다 몇 % 좋았는가? 그 정도 차이면 논문 한 편이 되지 않는가?
4. horizon 24h와 168h 중 어느 쪽이 어려운가? 몇 배 어려운가?

---

# 3주차. LightGBM 예측

## 학습목표

1. 시계열을 지도학습 문제로 변환하는 피처 설계를 한다.
2. 여러 건물을 하나의 모델로 학습하는 global model을 구성한다.
3. recursive와 direct 다단계 예측의 차이를 실험으로 확인한다.

## 배경 개념

### 왜 LightGBM부터인가

**이것이 현장의 실질 1순위이기 때문입니다.**

- CPU만으로 수 분 안에 학습됩니다. 다수 산업 현장은 GPU 없는 온프렘 서버입니다.
- 결측·범주형에 강건합니다.
- SHAP으로 근거를 설명할 수 있습니다. 현장 엔지니어를 설득하려면 이것이 필요합니다.
- M5 예측 대회 상위권의 주력이었습니다.

여러분이 나중에 배울 TSFM과 딥러닝 모델은 **이것을 이겨야 도입할 이유가 생깁니다.** 못 이기면 도입하지 않는 것이 옳은 판단입니다.

### 시계열 → 지도학습 변환

| 피처군                 | 예                                           |
| ---------------------- | -------------------------------------------- |
| **Lag**          | `y(t-1)`, `y(t-24)`, `y(t-168)`        |
| **Rolling 통계** | 최근 24h 평균/표준편차/최소/최대             |
| **캘린더**       | 시각, 요일, 월, 공휴일 여부                  |
| **주기 인코딩**  | `sin(2π·hour/24)`, `cos(2π·hour/24)` |
| **공변량**       | 외기온, 이슬점온도, 그 lag                   |
| **정적 피처**    | 건물 용도, 면적 (global model에서만 의미)    |

**lag 피처를 만들 때의 철칙**: horizon이 24h라면, 예측 시점에 `y(t-1)`은 알 수 없습니다. **가용한 최소 lag은 horizon과 같습니다.** 이것을 어기면 그 자체가 누수입니다.

> **⚠️ rolling 통계에도 같은 철칙이 적용됩니다 — 그리고 여기서 훨씬 많이 틀립니다.**
>
> `y.rolling(24).mean()`은 **현재 시점 `t`를 포함**합니다. 즉 `t`의 실제값이 피처에 섞여 들어갑니다. lag은 눈에 보이지만 이건 안 보이므로 조용히 통과합니다.
>
> ```python
> y.rolling(24).mean()                    # ❌ t 포함 — 누수
> y.shift(horizon).rolling(24).mean()     # ⭕ horizon 만큼 밀어낸 뒤 집계
> ```
>
> **모든 rolling 통계(평균·표준편차·최소·최대)에 `shift(horizon)`을 먼저 걸으십시오.** 이 한 줄을 빠뜨리면 3주차 점수가 비정상적으로 좋아지고, 그 원인을 5주차까지 못 찾습니다.

### Global model

건물마다 모델을 따로 만들지(local) 전부 한 모델로 학습할지(global)의 선택입니다.

|                    | Local             | Global                                     |
| ------------------ | ----------------- | ------------------------------------------ |
| 모델 수            | 건물 수만큼       | 1개                                        |
| 데이터가 적은 건물 | 학습 어려움       | **다른 건물에서 배운 패턴을 빌려옴** |
| 건물 고유 특성     | 잘 반영           | 정적 피처로 구분해야 함                    |
| 운영               | 수백 개 모델 관리 | **1개 관리**                         |

본 과정은 global을 기본으로 하되, local과 비교합니다.

### Recursive vs Direct

24시간을 예측하는 두 방식입니다.

|         | Recursive                                        | Direct                            |
| ------- | ------------------------------------------------ | --------------------------------- |
| 방식    | 1스텝 모델을 24번 반복, 예측값을 입력으로 재사용 | horizon별로 별도 모델 (h=1,…,24) |
| 모델 수 | 1                                                | 24                                |
| 문제    | **오차가 누적됨**                          | 학습 비용 24배                    |

## 실습 과제

**과제 3-1. 피처 파이프라인**

- 위 피처군을 생성하는 함수를 작성한다.
- **horizon에 따라 사용 가능한 lag이 달라지도록** 파라미터화한다.

**과제 3-2. Global model 학습**

- 선정한 10~20개 건물을 하나의 LightGBM으로 학습한다.
- 2주차 하네스에 끼워 넣어 백테스트를 실행한다.

**과제 3-3. Local vs Global 비교**

- 건물별 개별 모델도 학습해 같은 하네스로 평가한다.
- 어떤 건물에서 global이 유리하고 어떤 건물에서 local이 유리한가?

**과제 3-4. Recursive vs Direct 비교**

- horizon 24h에 대해 두 방식을 모두 구현해 비교한다.
- **오차가 horizon에 따라 어떻게 증가하는지 그래프로** 그린다.

**과제 3-5. 피처 중요도 해석**

- 상위 10개 피처를 뽑고, **왜 그것이 중요한지 물리적으로 설명**한다.
- "중요도가 높으니까 중요합니다"는 해석이 아닙니다.

**과제 3-6. 외삽 실험**

- 학습 구간보다 소비가 큰 기간을 테스트로 잡고 예측해 본다.
- LightGBM이 학습 범위를 넘어가지 못하는 현상을 확인한다.

## 산출 기준

- [ ] 피처 파이프라인 (horizon 파라미터화)
- [ ] Global model 백테스트 점수 + **Seasonal Naive 대비 개선율(%)**
- [ ] Local vs Global 비교표
- [ ] Recursive vs Direct 비교 + horizon별 오차 증가 그래프
- [ ] 피처 중요도 상위 10개와 **물리적 해석**
- [ ] 외삽 실험 결과
- [ ] 주간 보고서

## 내 결과가 정상인가 — 판정 기준

| 관찰                                        | 판정                                                                 |
| ------------------------------------------- | -------------------------------------------------------------------- |
| MASE 0.6~0.9 (Naive 대비 10~40% 개선)     | 정상 범위                                                            |
| **MASE 0.3 미만**                     | **누수를 의심하십시오.** ① lag이 horizon보다 짧은지 ② rolling 통계에 `shift(horizon)`을 걸었는지 — 이 둘을 즉시 검증 |
| MASE 1.0 이상                               | 피처 설계 문제. 주기 피처(sin/cos, 요일)가 있는지 확인               |
| Direct가 horizon 후반에서 Recursive보다 우세 | 정상 (교과서적 결과)                                                 |
| 피처 중요도 1~3위에 lag 또는 rolling       | 정상                                                                 |
| **피처 중요도에 lag이 하나도 없음**   | horizon 제약으로 전부 제거되었을 가능성이 큽니다. 예측이 평평해집니다 |

> **가장 위험한 칸은 두 번째 줄입니다.** 여러분은 나쁜 결과를 의심하도록 훈련되어 있지만, **이 과정에서 진짜 위험한 것은 너무 좋은 결과**입니다. MASE가 0.3이면 기뻐하지 말고 코드를 여십시오.

## 자가 점검 질문

1. 내가 만든 lag 피처 중 horizon보다 짧은 것이 하나라도 있는가?
2. Seasonal Naive를 몇 % 이겼는가? 못 이겼다면 원인은?
3. Global이 유리한 건물의 공통점은 무엇인가?
4. Direct가 Recursive보다 나은 정도는 학습 비용 24배를 정당화하는가?

---

# 4주차. LightGBM 이상진단 + SHAP 설명

## 학습목표

1. 예측잔차 기반 이상진단기를 만든다.
2. 고정 임계값과 동적 임계값의 차이를 실험으로 확인한다.
3. SHAP으로 이상 판정의 근거를 제시한다.

## 배경 개념

### 예측잔차 기반 이상진단

```
1. 정상 구간만으로 예측 모델을 학습한다
2. 전체 구간에 대해 예측한다
3. 잔차 = |실제값 - 예측값| 를 계산한다
4. 잔차가 임계값을 넘으면 이상으로 판정한다
```

이 방식의 논리는 **"정상만 학습한 모델은 정상을 잘 맞히고 이상을 못 맞힌다"**입니다.

**3단계에서 가장 중요한 것은 1번입니다.** 이상이 섞인 구간까지 학습에 넣으면 모델이 이상을 정상 패턴으로 학습해 버려서, 정작 그 이상을 탐지하지 못합니다. LEAD 1.0의 라벨을 이용해 정상 구간만 골라 학습하십시오.

### 임계값 — 실전의 진짜 병목

논문은 대개 "최적 임계값에서의 F1"을 보고합니다. 그러나 **현장에서는 정답 라벨이 없으므로 최적 임계값을 알 수 없습니다.**

| 방식                 | 정의                    | 문제                                |
| -------------------- | ----------------------- | ----------------------------------- |
| 고정 절대값          | 잔차 > 100 kWh          | 건물 규모가 다르면 무의미           |
| 고정 백분위          | 잔차 상위 1%            | **이상률이 항상 1%로 고정됨** |
| 표준편차 배수        | 잔차 > μ + 3σ         | 잔차 분포가 정규분포가 아님         |
| **동적(롤링)** | 최근 N일 잔차 분포 기준 | 계절 변화에 적응. 본 과정 권장      |

### SHAP으로 근거 설명하기

이상을 탐지했다고 현장이 움직이지 않습니다. **"왜 이상인가"**에 답해야 합니다.

SHAP은 각 피처가 예측값을 얼마나 밀어올리거나 끌어내렸는지 수치로 분해합니다. 이상 시점의 SHAP 값을 보면 이렇게 말할 수 있습니다.

> "이 시점의 예측값은 평소보다 낮았습니다. 외기온 피처가 -30 kWh, 요일 피처가 -15 kWh 기여했습니다. 그러나 실제 소비는 예측보다 200 kWh 높았습니다. 즉 **기상·일정으로 설명되지 않는 소비 증가**입니다."

이것이 공고 주제인 「예측 근거 설명」의 1차 형태입니다. 9주차와 11주차에 두 번 더 심화합니다.

## 실습 과제

**과제 4-1. LEAD 1.0 데이터 파악 및 BDG2 조인**

- 라벨 구조, 이상 비율, 이상 구간의 길이 분포를 확인한다.
- BDG2와 형식이 어떻게 다른지 정리한다. **건물 수와 기간을 반드시 비교할 것.**
- **BDG2 서브셋과 조인한다.** 키는 `metadata.csv`의 `building_id_kaggle` ↔ LEAD의 `building_id` (0.4절 참조).
  - **조인 전 dtype을 맞춘다.** float ↔ int64를 그대로 merge하면 오류 없이 0건이 나온다
  - **조인 직후 매칭된 건물 수를 출력해 확인한다.** 1주차 서브셋 전부가 매칭되어야 정상이다
- **1주차에 왜 후보 목록 안에서만 서브셋을 고르게 했는지 이 시점에 설명할 수 있어야 한다.** 보고서에 1문단으로 쓴다.

> **기간 주의** — LEAD 라벨은 **2016년 1년치뿐**입니다. BDG2는 2016~2017 2년입니다. 따라서 4주차의 학습·평가 구간은 **2016년으로 한정**됩니다.
>
> **4주차만 윈도우를 4개로 줄이십시오.** 2주차 하네스는 최소 8개를 전제했지만, 기간이 절반이므로 같은 학습창 길이로 8개를 잡으면 윈도우가 겹치거나 학습 구간이 지나치게 짧아집니다. **하네스 코드는 바꾸지 말고 `n_windows` 인자만 4로 주십시오.** 하네스 자체를 고치면 1~3주차 결과와의 비교가 깨집니다.
>
> 그리고 **1~3주차와 조건이 다르다는 사실(기간 2016년 한정, 윈도우 4개)을 보고서에 반드시 명시**하십시오. 4주차 점수를 3주차 점수와 나란히 놓고 비교하면 안 됩니다.

**과제 4-2. 정상 구간만으로 학습**

- 라벨을 이용해 정상 구간만 추출해 LightGBM을 학습한다.
- **비교군**: 전체 구간(이상 포함)으로 학습한 모델도 만든다.

**과제 4-3. 잔차 기반 탐지**

- 두 모델의 잔차를 각각 계산하고, 이상 구간에서 잔차가 얼마나 커지는지 비교한다.
- **오염 학습이 탐지 성능을 얼마나 떨어뜨리는지 정량화한다.**

**과제 4-4. 임계값 4안 비교**

- 위 표의 4가지 방식을 모두 구현한다.
- Precision·Recall·F1을 계산해 비교한다.
- **"라벨이 없다면 어느 방식을 쓰겠는가"에 답한다.**

**과제 4-5. SHAP 근거 리포트**

- 탐지된 이상 이벤트 **3건**을 골라 SHAP 값을 계산한다.
- 각 건에 대해 위 예시 형태의 **설명문을 사람이 읽을 수 있는 문장으로** 작성한다.

## 산출 기준

- [ ] LEAD 1.0 데이터 프로파일 (건물 수·기간·이상률 포함)
- [ ] **BDG2 서브셋과의 조인 결과 — 매칭된 건물 수 확인**
- [ ] 1주차 후보 목록 제한의 이유 설명 1문단
- [ ] 정상학습 vs 오염학습 모델 비교 (잔차 분포 그래프)
- [ ] **오염 학습으로 인한 탐지 성능 저하 정량값**
- [ ] 임계값 4안 비교표 (Precision/Recall/F1)
- [ ] 라벨 없는 상황에서의 임계값 선택 방침과 근거
- [ ] **이상 이벤트 3건 SHAP 설명문**
- [ ] 주간 보고서

## 내 결과가 정상인가 — 판정 기준

| 관찰                                                        | 판정                                                        |
| ----------------------------------------------------------- | ----------------------------------------------------------- |
| **LEAD 조인 결과가 0건**                              | **dtype 불일치입니다.** 1주차 후보 목록에서 골랐다면 반드시 매칭됩니다. 부록 C.1 참조 |
| **오염 학습 모델의 이상 구간 잔차가 정상 학습 대비 유의하게 작음** | 정상 (이것이 과제 4-3의 요점)                               |
| 오염 학습과 정상 학습의 차이가 거의 없음                    | 이상 비율이 너무 낮아 학습에 영향이 없었을 가능성. 서브셋의 실제 이상률을 확인 |
| 동적(롤링) 임계값이 고정 백분위보다 나음                    | 흔한 결과                                                   |
| **어떤 임계값에서도 F1이 매우 낮음**                  | 잔차 기반 방식 자체가 이 이상 유형에 안 맞을 수 있습니다. 9주차에서 다시 다룹니다 |

## 자가 점검 질문

1. 오염 학습 모델은 왜 탐지를 못 했는가? 잔차 그래프로 설명할 수 있는가?
2. 내 임계값 방식은 다음 달 데이터에서도 작동할 것 같은가?
3. SHAP 설명문을 설비 담당자가 읽으면 무엇을 하러 갈 것 같은가? 행동 가능한 설명인가?
4. Precision과 Recall 중 이 문제에서 무엇이 더 중요한가? 왜인가?

---

# 5주차. TSFM 입문 — Chronos-2 zero-shot

## 학습목표

1. TSFM의 zero-shot 예측을 실행한다.
2. 공변량 투입 전후의 성능 차이를 측정한다.
3. **Naive / LightGBM / Chronos-2를 정면 비교하고 판정한다.**

> **이 주가 1단계의 클라이맥스입니다.** 학습 데이터 0으로 어디까지 되는지, 3주간 공들여 만든 LightGBM을 이기는지 직접 확인합니다.

## 배경 개념

### 시계열 파운데이션 모델(TSFM)이란

대규모 시계열 코퍼스로 사전학습된 모델로, **대상 데이터로 학습하지 않고(zero-shot) 바로 예측**합니다. LLM이 새 문서를 학습하지 않고 요약하는 것과 같은 구도입니다.

| 모델                         | 규모  | 특징                                                            |
| ---------------------------- | ----- | --------------------------------------------------------------- |
| **Chronos-2** (Amazon) | ~120M | GIFT-Eval 프리트레인 부문 1위.**다변량·외생공변량 지원** |
| TimesFM 2.5 (Google)         | ~200M | 사전학습 코퍼스가 달라 교차 검증용으로 유용                     |
| Moirai 2.0 (Salesforce)      | 소형  | **본 과정에서는 사용 금지** (아래 참조)                   |
| TTM (IBM)                    | 1~5M  | 극소형. CPU·엣지용. 6주차에 사용                               |

### 왜 Moirai를 쓰지 않는가 — 사전학습 오염

Moirai 계열은 LOTSA라는 대형 코퍼스로 학습되었고, **LOTSA에는 BDG2가 포함되어 있습니다.**

즉 Moirai에게 BDG2는 zero-shot이 아니라 이미 본 데이터입니다. 그 위에서 "zero-shot 성능이 좋다"고 말하면 시험 문제를 미리 본 학생의 점수를 실력이라고 부르는 것과 같습니다.

반면 **Chronos-2와 TimesFM은 BDG2를 사전학습에 포함하지 않은 것으로 확인**되어 비교가 유효합니다.

**이것이 TSFM 시대의 가장 중요한 함정입니다.** 7주차에 여러분이 직접 각 모델의 학습 코퍼스 문서를 읽고 검증하게 됩니다.

### 공변량(covariate)

외기온처럼 **미래 값을 알 수 있는 외부 변수**입니다. 기상 예보가 있으므로 내일의 외기온은 알 수 있습니다.

전통적으로 TSFM은 공변량을 다루지 못하는 것이 최대 약점이었습니다. Chronos-2는 이 부분을 개선했고, 실제로 공변량이 있는 과제에서 이전 모델들과의 격차가 가장 크게 벌어집니다.

**LightGBM은 공변량을 열 하나 추가하는 것으로 자연스럽게 처리합니다.** 이 지점이 두 계열의 정면 승부처입니다.

## 실습 과제

**과제 5-1. Chronos-2 zero-shot 실행**

- 모델 카드를 읽고 로딩·추론 API를 확인한다. **API는 버전에 따라 다르므로 모델 카드를 신뢰하십시오.**
- Chronos-2는 `transformers`가 아니라 **`chronos-forecasting` 패키지**로 로드합니다. 진입점은 `BaseChronosPipeline.from_pretrained("amazon/chronos-2", device_map="cpu")` 입니다.
- 파이프라인은 **단일 계열 예측(`predict`)과 데이터프레임 예측(`predict_df`)** 두 경로를 제공합니다. 공변량(과제 5-3)은 `predict_df` 쪽에서 다룹니다. 어느 경로를 쓸지 먼저 정하십시오.
- 2주차 하네스의 `model_fn`/`predict_fn` 인터페이스에 맞춰 래핑한다.
- 학습 없이 예측하므로 `model_fn`은 아무것도 학습하지 않습니다. 컨텍스트 길이만 결정합니다.

**과제 5-2. 컨텍스트 길이 민감도**

- 입력 컨텍스트를 168h / 512h / 1024h로 바꿔가며 점수를 비교한다.
- **길수록 좋은가? 어디서 포화되는가?**

**과제 5-3. 공변량 투입**

- 외기온을 공변량으로 넣은 경우와 넣지 않은 경우를 비교한다.
- 개선폭을 정량화한다.

**과제 5-4. 3-way 정면 비교** ⭐

- 같은 하네스, 같은 건물, 같은 윈도우로 다음을 비교한다.

| 모델                | horizon 24h | horizon 168h |
| ------------------- | ----------- | ------------ |
| Seasonal Naive      | (2주차 값)  | (2주차 값)   |
| LightGBM (global)   | (3주차 값)  | (3주차 값)   |
| Chronos-2 zero-shot |             |              |
| Chronos-2 + 외기온  |             |              |

- **건물별로도 승패를 집계한다.** 어떤 건물에서 어느 쪽이 이기는가?

**과제 5-5. 승패 원인 분석**

- 이긴 쪽이 이긴 이유, 진 쪽이 진 이유를 데이터로 설명한다.
- 힌트: 이력 길이, 주기성의 강도, 결측률, 소비 규모, 용도

**과제 5-6. CPU 추론 시간 측정** — 측정 프로토콜 고정

"몇 배 느린가"는 **비교 방식을 정하지 않으면 답이 없습니다.** 같은 데이터에서 3배가 나오기도 하고 70배가 나오기도 합니다. 그러므로 다음 프로토콜을 그대로 지키고, 결과를 이 표 형태로 제출하십시오.

**측정 조건 (전원 동일하게 고정)**

- 건물 1개, horizon 24h
- **3회 이상 반복 후 중앙값**. 첫 호출은 워밍업이므로 버린다
- 모델 로딩 시간은 제외. **추론 시간만** 측정
- `torch.set_num_threads(N)`으로 스레드 수를 명시적으로 고정하고 **N을 기록**한다

**제출 표**

| 방식                              | ms / 건물 | 비고                        |
| --------------------------------- | --------- | --------------------------- |
| LightGBM — recursive (24스텝 순차) |           | 1행씩 24번 호출             |
| LightGBM — direct (24행 배치)      |           | 1번 호출                    |
| Chronos-2 zero-shot               |           | 컨텍스트 길이 기록          |
| (참고) 학습 시간                  |           | LightGBM만 해당. Chronos-2는 0 |

- **배수는 두 개를 모두 적는다**: Chronos-2 ÷ LightGBM(recursive), Chronos-2 ÷ LightGBM(direct).
- 두 배수의 차이가 왜 그렇게 큰지 1문단으로 설명한다. **이것이 이 과제의 진짜 질문입니다.**
- 6주차 과제 6-2의 "추론 ms/계열" 칸은 여기서 정한 방식과 **같은 방식으로** 채운다.

## 산출 기준

- [ ] Chronos-2가 2주차 하네스에서 동작
- [ ] 컨텍스트 길이 민감도 그래프
- [ ] 공변량 투입 전후 비교
- [ ] **3-way(4행) 비교표 — horizon 2종, 건물별 승패 집계 포함**
- [ ] 승패 원인 분석 1~2문단 (데이터 근거 필수)
- [ ] **CPU 추론 시간 비교 표 — 스레드 수·반복 횟수·배수 2종 기록**
- [ ] 주간 보고서

## 내 결과가 정상인가 — 판정 기준

| 관찰                                                | 판정                                                        |
| --------------------------------------------------- | ----------------------------------------------------------- |
| Chronos-2 zero-shot이 Seasonal Naive를 이김         | 정상 (거의 항상)                                            |
| **Chronos-2가 LightGBM에 짐**                 | **정상.** 이력이 충분한 데이터에서는 흔한 결과입니다. 실패가 아닙니다 |
| Chronos-2가 LightGBM을 이김                         | 정상. 어떤 건물에서 이겼는지 원인을 분석하십시오            |
| 공변량 투입으로 개선                                | 정상                                                        |
| 컨텍스트 길이를 늘리면 어느 지점에서 성능이 포화    | 정상                                                        |
| **CPU 추론이 LightGBM(recursive) 대비 2~5배 느림** | **정상.** 검산값 — Chronos-2 67.9ms vs LightGBM recursive 25.2ms |
| **CPU 추론이 LightGBM(direct 배치) 대비 50~100배 느림** | **정상.** 검산값 — 67.9ms vs 1.0ms                    |
| **건물당 추론이 1초를 넘음**                  | **비정상.** 자원이 아니라 구현 문제입니다. 부록 C.3의 4단계 점검 |

> **두 배수가 모두 정상입니다.** 어느 쪽과 비교했는지 밝히지 않은 배수는 쓸 수 없습니다 — 이 숫자가 6주차 배포 판단의 근거가 되기 때문입니다.

## 자가 점검 질문

1. Chronos-2가 LightGBM을 이겼는가? **어느 조건에서** 이겼는가?
2. 공변량을 넣은 Chronos-2와 LightGBM 중 어느 쪽이 외기온을 더 잘 쓰는가?
3. 만약 Chronos-2가 졌다면, 그것은 모델이 나쁘다는 뜻인가 아니면 다른 뜻인가?
4. 추론이 N배 느리다면, 성능 개선폭이 그것을 정당화하는가? 어떤 현장에서 정당화되고 어떤 현장에서 안 되는가?

---

# 6주차. 1단계 통합 및 중간 발표

## 학습목표

1. 경량 TSFM(TTM)의 CPU 실행 가능성을 확인한다.
2. CPU 환경의 추론 지연을 벤치마크한다.
3. **"GPU 없는 현장에 무엇을 배포할 것인가"에 답한다.**

## 배경 개념

### 경량 모델의 존재 이유

Chronos-2는 ~120M 파라미터입니다. IBM의 TTM은 1~5M입니다. **20~100배 작습니다.**

작은 모델이 왜 필요한가? 현장에는 이런 제약이 있습니다.

- 설비 옆 산업용 PC에서 돌려야 한다 (엣지)
- 수천 개 계열을 1분 안에 다 예측해야 한다
- 서버 증설 예산이 없다

이때 "성능이 조금 낮지만 100배 빠른 모델"이 정답인 경우가 많습니다.

### 배포 판단의 축

기업에서 모델을 고를 때 보는 것은 정확도만이 아닙니다.

| 축         | 질문                                                |
| ---------- | --------------------------------------------------- |
| 정확도     | Naive 대비 몇 % 개선인가                            |
| 지연       | 계열당 몇 ms인가                                    |
| 자원       | RAM 얼마, GPU 필요한가                              |
| 운영       | 재학습 주기는? 계열이 늘면 비용이 어떻게 늘어나는가 |
| 설명       | 왜 그렇게 예측했는지 답할 수 있는가                 |
| 콜드스타트 | 신규 설비에 즉시 투입 가능한가                      |

**1단계 종합 보고서는 이 6축으로 모델들을 평가하는 문서입니다.**

## 실습 과제

**과제 6-1. TTM 적용**

- TTM(`granite-tsfm` 패키지, `ibm-granite/granite-timeseries-ttm-r2`)을 하네스에 끼워 넣어 백테스트한다.
- Chronos-2 대비 정확도와 속도를 비교한다.

> **⚠️ TTM은 horizon마다 다른 사전학습 리비전으로 라우팅됩니다.** 이것이 이 과제의 함정입니다.
>
> TTM은 Chronos-2처럼 임의 horizon을 받는 모델이 아닙니다. 요청한 `prediction_length`에 따라 내부적으로 **서로 다른 체크포인트**가 선택되고, 그중 일부는 추가 입력을 요구합니다. 본 과정의 필수 horizon 24h에서 다음 오류를 만나게 됩니다.
>
> ```
> Exception: Expecting freq_token in forward
> ```
>
> 이것은 버그가 아니라 **주파수 프리픽스 튜닝(frequency prefix tuning) 리비전이 선택되었다는 신호**입니다. 모델 카드와 `get_model()`의 인자를 읽고, 시간 단위(hourly) 데이터임을 모델에 알려주는 방법을 찾으십시오. 168h에서는 이 오류가 나지 않습니다. **왜 24h에서만 나는지**를 함께 설명하십시오.
>
> 아울러 요청한 horizon이 체크포인트의 고유 예측 길이와 정확히 일치하지 않으면 경고와 함께 잘라내기(prediction filter)가 적용됩니다. **이 경고가 떴는지 여부를 보고서에 기록**하십시오. 잘라내어 얻은 24h 예측은 24h 전용으로 학습된 모델의 예측과 같지 않습니다.

**과제 6-2. 전체 벤치마크**

- 지금까지의 모든 모델에 대해 다음을 측정한다.
- **추론 시간은 과제 5-6에서 고정한 프로토콜과 같은 방식으로 측정한다.** 표 아래에 어느 방식(recursive/direct)인지와 스레드 수를 반드시 적는다.
- 최대 RAM은 `psutil.Process().memory_info().rss` 를 사용한다. **모델 로딩 전 baseline과 추론 중 피크를 각각** 기록한다.

| 모델               | MASE(24h) | MASE(168h) | 추론 ms/계열 | 학습 시간 | 최대 RAM |
| ------------------ | --------- | ---------- | ------------ | --------- | -------- |
| Seasonal Naive     |           |            |              | —        |          |
| LightGBM (local)   |           |            |              |           |          |
| LightGBM (global)  |           |            |              |           |          |
| Chronos-2          |           |            |              | —        |          |
| Chronos-2 + 공변량 |           |            |              | —        |          |
| TTM                |           |            |              | —        |          |

> 측정 방식: ____________ (recursive / direct), 스레드 수: ____, 반복 횟수: ____

**과제 6-3. 6축 배포 판단표**

- 위 6개 축으로 각 모델을 평가한 표를 만든다.
- **시나리오 3개**에 대해 각각 어느 모델을 배포할지 고르고 근거를 쓴다.
  - 시나리오 A: 이력 2년 보유, GPU 없는 온프렘 서버, 건물 50개
  - 시나리오 B: 신규 준공 건물, 이력 2주, 즉시 예측 필요
  - 시나리오 C: 설비 옆 산업용 PC(RAM 4GB, **2코어**), **계열 500개를 1분 주기로 전부** 예측

> 시나리오 C는 "RAM에 안 올라간다"로 답하지 마십시오. **과제 6-2에서 직접 측정한 RAM 값으로 확인**하고, 실제로 무엇이 제약이 되는지를 숫자로 판정하십시오.

**과제 6-4. 1단계 종합 보고서 작성**

- 구성: 문제 정의 → 데이터·평가 설계 → 모델별 결과 → 6축 판단표 → 시나리오별 권고 → 1단계 결론 → 2단계 계획

**과제 6-5. 발표 (20분 + 질의 10분)**

## 산출 기준

- [ ] TTM 백테스트 결과
- [ ] **전체 벤치마크 표 (6개 모델 × 5개 측정값)**
- [ ] 6축 배포 판단표
- [ ] **시나리오 3개에 대한 모델 선택과 근거**
- [ ] 1단계 종합 보고서 — **이번 주 주간 보고서를 대체**
- [ ] 발표 자료 및 발표 수행

## 내 결과가 정상인가 — 판정 기준

| 관찰                                              | 판정                                                        |
| ------------------------------------------------- | ----------------------------------------------------------- |
| TTM이 Chronos-2보다 정확도는 낮고 속도는 빠름     | 정상                                                        |
| **TTM이 Chronos-2보다 30~40배 빠름**       | 정상. 검산값 — TTM 2.0ms vs Chronos-2 67.9ms               |
| **TTM 피크 RAM이 Chronos-2의 절반 수준**    | 정상. 검산값 — 약 0.5GB vs 약 1.0GB. **둘 다 4GB에 올라갑니다** |
| 시나리오 A/B/C의 답이 서로 다름                   | **정상이자 목표**                                     |
| 세 시나리오의 답이 모두 같음                      | 다시 생각하십시오. 특히 B(이력 2주)와 C(500계열·1분 주기)는 답이 달라야 합니다 |
| **"Chronos-2는 4GB에 안 올라가서 탈락"이라고 씀** | **틀렸습니다.** 여러분이 과제 6-2에서 측정한 값과 모순됩니다. 처리량으로 논증하십시오 |
| **TTM 행의 MASE(24h)가 비어 있음**          | **미완입니다.** `freq_token` 함정에서 포기한 것이므로 채워야 합니다 |

## 자가 점검 질문

1. 세 시나리오에서 같은 모델을 골랐는가, 다른 모델을 골랐는가? 다르다면 무엇이 결정적이었는가?
2. 내 결론은 "최신 모델이 좋다"인가 "조건에 따라 다르다"인가? 후자라면 조건을 정확히 말할 수 있는가?
3. 이 결과를 회사 임원에게 5분 안에 설명한다면 무엇을 말하겠는가?

---

# 2단계 — 서버(GPU) 6주

> **2단계의 목표는 "심화하고 시스템으로 만드는 것"입니다.** GPU가 있어야 가능한 것을 하고, 마지막에 전부 하나로 묶습니다.

---

# 7주차. TSFM 심화 — LoRA 파인튜닝과 오염 검증

## 학습목표

1. LoRA로 TSFM을 파인튜닝한다.
2. few-shot 데이터 효율 곡선을 측정한다.
3. **사전학습 오염을 직접 검증한다.**

## 배경 개념

### LoRA (Low-Rank Adaptation)

모델 전체를 재학습하지 않고, **각 가중치 행렬에 작은 저랭크 행렬을 더해 그것만 학습**합니다.

|               | 전체 파인튜닝 | LoRA                 |
| ------------- | ------------- | -------------------- |
| 학습 파라미터 | 전부 (~120M)  | 일부 (~0.1~1%)      |
| VRAM          | 큼            | **8GB로 충분** |
| 원본 모델     | 덮어씀        | 보존. 어댑터만 교체  |

어댑터가 작으므로 **건물별·설비별 어댑터를 따로 두고 갈아 끼우는 운영**이 가능합니다.

### 파인튜닝 시 정상 구간만 학습해야 하는 이유

4주차에서 다룬 것과 같습니다. 이상이 섞인 데이터로 파인튜닝하면 모델이 이상 패턴을 정상으로 학습해, 이후 그 이상을 탐지하지 못합니다.

### few-shot 데이터 효율

TSFM의 진짜 강점은 "zero-shot 성능"보다 **"적은 데이터로 빠르게 좋아지는 것"**일 수 있습니다.

학습 데이터를 1% / 5% / 10% / 50% / 100%로 바꿔가며 성능을 그리면 곡선이 나옵니다. LightGBM의 같은 곡선과 겹쳐 그리면, **"데이터가 얼마나 적을 때 TSFM이 유리한가"**의 손익분기점을 찾을 수 있습니다.

이것이 신규 설비·신축 건물에 대한 실무 답이 됩니다.

### 사전학습 오염 검증 — 이 주의 핵심

TSFM 논문의 zero-shot 성능 주장을 검증하는 절차입니다.

```
1. 해당 모델의 논문·모델카드에서 사전학습 코퍼스 목록을 찾는다
2. 목록에 평가 데이터셋이 포함되는지 확인한다
3. 포함된다면 → zero-shot 비교에서 배제한다
4. 포함되지 않는다면 → 근거(문서의 어느 부분)를 기록한다
```

**이 절차를 문서로 남기는 것이 이 주의 가장 중요한 산출물입니다.** 나중에 회사에서 "이 모델 써도 됩니까"라는 질문을 받았을 때 답할 수 있는 유일한 방법입니다.

## 실습 과제

**과제 7-1. 서버 환경 확인**

- `nvidia-smi`로 GPU 확인, `requirements_stage2.txt` 설치
- 5주차 Chronos-2 실험을 GPU에서 재현. **CPU 대비 속도 비교**

**과제 7-2. 오염 검증 문서 작성** ⭐

- Chronos-2 / TimesFM 2.5 / Moirai 2.0 세 모델에 대해 위 4단계 절차를 수행한다.
- 결과를 표로 정리한다.

| 모델        | 코퍼스 문서 위치 | BDG2 포함 여부 | 근거 | 본 과정 사용 가부 |
| ----------- | ---------------- | -------------- | ---- | ----------------- |
| Chronos-2   |                  |                |      |                   |
| TimesFM 2.5 |                  |                |      |                   |
| Moirai 2.0  |                  |                |      |                   |

**과제 7-3. LoRA 파인튜닝**

- 정상 구간만으로 Chronos-2를 LoRA 파인튜닝한다.
- zero-shot 대비 개선율을 측정한다.
- **학습 파라미터 수와 VRAM 사용량을 기록한다.**

**과제 7-4. 오염 학습 대조 실험**

- 이상 포함 데이터로도 파인튜닝해 비교한다.
- 예측 성능과 (다음 주 쓸) 잔차 분포가 어떻게 달라지는가?

**과제 7-5. few-shot 효율 곡선**

- 학습 데이터 1%/5%/10%/50%/100%로 파인튜닝을 반복한다.
- **LightGBM의 같은 곡선과 겹쳐 그린다.**
- 손익분기점(데이터가 X% 이하면 TSFM 유리)을 찾는다.

## 산출 기준

- [ ] GPU 환경 구축, CPU 대비 속도 비교
- [ ] **오염 검증 표 3개 모델 — 근거 문서 위치 명시**
- [ ] LoRA 파인튜닝 결과: zero-shot 대비 개선율, 학습 파라미터 수, VRAM
- [ ] 오염 학습 대조 실험 결과
- [ ] **few-shot 효율 곡선 (TSFM vs LightGBM 겹쳐 그리기)**
- [ ] 손익분기점 수치와 해석
- [ ] 주간 보고서

## 내 결과가 정상인가 — 판정 기준

| 관찰                                                  | 판정                                                    |
| ----------------------------------------------------- | ------------------------------------------------------- |
| LoRA 학습 파라미터가 전체의 0.1~1%                   | 정상                                                    |
| VRAM 8GB 이내                                         | 정상                                                    |
| **zero-shot 대비 개선율이 한 자릿수 %**         | **정상** (이력이 충분할 때). few-shot 곡선의 왼쪽에서 이득이 큽니다 |
| few-shot 곡선에서 데이터가 적을수록 TSFM 우위         | 정상이자 기대 결과                                      |
| **손익분기점이 존재하지 않음 (LightGBM이 항상 우위)** | 가능한 결과입니다. 데이터·서브셋 특성으로 설명하십시오 |
| 코퍼스 목록을 못 찾음                                 | **그것도 결과입니다.** "코퍼스 미공개 → 오염 판단 불가"는 정당한 결론이고, 실무에서는 리스크로 판정합니다 |

## 자가 점검 질문

1. 오염 검증 표의 "근거" 칸을 다른 사람이 읽고 스스로 확인할 수 있는가?
2. LoRA 개선율이 나왔다면, 그것이 진짜 개선인지 어떻게 확신하는가?
3. few-shot 손익분기점은 몇 %인가? 그것은 실제 건물로 치면 몇 개월치 데이터인가?
4. 만약 Moirai를 배제하지 않고 썼다면 결과가 어떻게 보였을까? 그 결과를 보고서에 썼다면 무슨 일이 일어났을까?

---

# 8주차. 예측 전용 딥러닝

## 학습목표

1. Time-Series-Library로 최신 딥러닝 모델 4종을 동일 조건에서 비교한다.
2. look-back 길이 민감도를 측정한다.
3. **논문 SOTA의 재현성 문제를 직접 확인한다.**

> **이 주의 범위**: 모델을 직접 구현하지 않습니다. 1주(8시간)에 4개 모델을 구현하는 것은 불가능하고, 그것이 이 주의 목적도 아닙니다. **비교 실행과 해석에 집중합니다.**

## 배경 개념

### 대상 모델 4종

| 모델                   | 발표       | 핵심 아이디어                                                             |
| ---------------------- | ---------- | ------------------------------------------------------------------------- |
| **PatchTST**     | ICLR'23    | 시계열을 패치로 잘라 토큰화. 패칭의 원형                                  |
| **iTransformer** | ICLR'24    | **변수를 토큰으로** 취급 (시점이 아니라). 장기예측 SOTA로 자주 인용 |
| **TimeXer**      | NeurIPS'24 | **외생변수 활용**을 정식으로 다룸. 계산 효율 좋음                   |
| **DLinear**      | AAAI'23    | **선형 한 층.** 위 모델들을 자주 이기는 베이스라인                  |

### DLinear가 왜 중요한가

DLinear는 추세와 계절성을 분해한 뒤 각각에 선형 레이어 하나를 적용합니다. 그게 전부입니다.

이 단순한 모델이 복잡한 트랜스포머들을 이기는 경우가 흔합니다. 이 사실이 알려진 뒤 시계열 딥러닝 논문은 반드시 DLinear와 비교하게 되었습니다.

**여러분이 이 주에 확인할 것은 그것이 여러분의 데이터에서도 사실인가입니다.**

### look-back 민감도 — 재현성 문제의 핵심

look-back은 모델이 보는 과거 길이입니다. 96 / 336 / 512 등을 씁니다.

논문마다 이 값을 다르게 설정하는데, **look-back을 바꾸면 모델 순위가 뒤바뀌는 경우가 많습니다.** 즉 "A가 B보다 좋다"는 주장이 특정 설정에서만 성립할 수 있습니다.

이것이 시계열 딥러닝 분야의 재현성 논란의 핵심이고, **여러분이 논문을 읽을 때 반드시 확인해야 할 항목**입니다.

## 실습 과제

**과제 8-1. Time-Series-Library 설치와 데이터 어댑터**

- `thuml/Time-Series-Library` 저장소를 클론한다.
- **BDG2를 이 라이브러리가 요구하는 형식으로 변환**하는 어댑터를 작성한다. (이 과제가 이번 주 작업의 절반입니다)

**과제 8-2. 4종 동일 조건 실행**

- PatchTST / iTransformer / TimeXer / DLinear를 같은 설정으로 학습·평가한다.
- **고정할 것**: 데이터, 분할, look-back, horizon, 에폭 상한, 시드
- 학습 시간과 VRAM도 함께 기록한다.

> **에폭 상한 10 · early stopping patience 3 · 서브셋은 1주차 그대로.** 임의로 늘리지 마십시오. 과제 8-3에서 look-back 3종을 다시 돌리므로 **총 12회(4모델 × 3 look-back)** 를 배정 시간 안에 끝내야 합니다. 성능 최고치를 뽑는 것이 목적이 아니라 **동일 조건 비교**가 목적입니다.

**과제 8-3. 민감도 실험 — 두 층에서** ⭐

이 과제는 **순위라는 것이 얼마나 쉽게 흔들리는지**를 확인하는 과제입니다. 흔드는 손잡이가 두 개 있고, 둘은 층이 다릅니다.

**(a) 모델 층 — look-back 민감도**

- look-back을 96 / 336 / 512로 바꿔가며 4종을 모두 재실행한다.
- **순위가 뒤바뀌는지 확인한다.**

| look-back | 1위 | 2위 | 3위 | 4위 |
| --------- | --- | --- | --- | --- |
| 96        |     |     |     |     |
| 336       |     |     |     |     |
| 512       |     |     |     |     |

**(b) 평가 층 — 하네스 설계 민감도** ⭐

look-back은 **모델의** 설정입니다. 그러나 순위를 흔드는 손잡이는 모델 밖에도 있습니다. **2주차에 여러분이 직접 고른 백테스트 설계 결정**이 그것입니다.

- look-back을 **336으로 고정**하고, **2주차 설계 결정 중 하나만** 반대로 바꿔 4종을 재실행한다.
  - 학습창 방식을 `expanding`으로 했다면 `sliding`으로 (또는 그 반대)
  - **하네스 코드는 고치지 말고 `train_mode` 인자만 바꾼다**
- 순위와 절대 점수가 어떻게 달라지는가?

| 하네스 설계          | 1위 | 2위 | 3위 | 4위 | 1위의 MASE |
| -------------------- | --- | --- | --- | --- | ---------- |
| 2주차 설계 (내 선택) |     |     |     |     |            |
| 반대 설계            |     |     |     |     |            |

- **두 표를 나란히 놓고 답하십시오.** 모델을 바꿔서 생긴 순위 변동과, **평가 설계를 바꿔서 생긴 순위 변동 중 어느 쪽이 컸는가?**
- 만약 후자가 크다면, 그것은 **2주차의 결정 하나가 8주간의 모델 비교 전체보다 결론에 더 크게 기여했다**는 뜻입니다. 이 사실을 12주차 최종 보고서 5절(네 가지 함정)에 반드시 포함하십시오.

> **왜 이것을 8주차에 하는가** — 이 과정은 "평가 설계가 모델 선택보다 먼저다"를 2주차부터 주장해 왔습니다. (b)가 그 주장의 **실측 증거**입니다. 주장을 믿으라고 하지 않고 직접 재보게 하는 것이 이 커리큘럼의 방식입니다.

**과제 8-4. TimeXer 공변량 실험**

- TimeXer에 외기온을 외생변수로 투입한 경우와 아닌 경우를 비교한다.
- 5주차 Chronos-2 공변량 실험과 개선폭을 비교한다.

**과제 8-5. 하네스 통합**

- 딥러닝 모델들의 예측 결과를 2주차 하네스의 tidy 형식으로 변환한다.
- **LightGBM·Chronos-2와 같은 표에 올린다.**

## 산출 기준

- [ ] BDG2 → Time-Series-Library 데이터 어댑터
- [ ] 4종 동일 조건 비교표 (성능 + 학습시간 + VRAM)
- [ ] **look-back 3종 × 모델 4종 순위표 — 순위 역전 여부 명시**
- [ ] **하네스 설계 2종 순위표 (과제 8-3b) — 모델 층 변동과 평가 층 변동의 크기 비교** ⭐
- [ ] TimeXer 공변량 실험, Chronos-2와 비교
- [ ] 누적 비교표에 딥러닝 4종 추가
- [ ] 주간 보고서

## 내 결과가 정상인가 — 판정 기준

| 관찰                                        | 판정                                                      |
| ------------------------------------------- | --------------------------------------------------------- |
| **DLinear가 상위권**                  | **정상. 매우 흔합니다.** "내가 뭘 잘못했나"가 아닙니다 |
| look-back 변경 시 순위 역전 발생            | **정상이자 기대 결과**                              |
| **하네스 설계를 바꿨더니 순위가 또 바뀜** | **정상이자 이 과제의 핵심.** 평가 설계가 모델 선택만큼, 때로는 그보다 크게 결론을 좌우합니다 |
| 하네스 설계를 바꿨는데 점수가 **전혀** 안 변함 | 인자가 실제로 적용되지 않았을 가능성. 학습창 길이·윈도우 시작점이 정말 달라졌는지 확인 |
| look-back을 바꿔도 순위가 완전히 고정       | 가능하지만 드묾. 세 설정이 실제로 다르게 적용됐는지 확인  |
| 딥러닝 4종이 LightGBM에 모두 짐             | 정상. 건물 10~20개 규모에서는 흔합니다                   |
| 학습 시간이 LightGBM의 수십~수백 배        | 정상. **이 숫자가 12주차 판단의 근거가 됩니다**     |

## 자가 점검 질문

1. DLinear가 몇 등이었는가? 예상과 달랐는가?
2. look-back에 따라 순위가 바뀌었는가? 바뀌었다면 논문의 "SOTA" 주장은 무엇을 의미하는가?
2-1. **하네스 설계를 바꿨을 때의 변동은 look-back을 바꿨을 때보다 컸는가, 작았는가?** 컸다면, 2주차의 나는 그 결정을 얼마나 신중하게 내렸는가?
3. 4종 중 가장 좋은 모델이 LightGBM을 이겼는가? 학습 시간과 GPU 요구를 감안하면 어떤가?
4. 만약 회사에서 "이 논문 모델 도입합시다"라는 제안을 받으면, 이제 무엇을 먼저 물어보겠는가?

---

# 9주차. 이상진단 전용 모델

## 학습목표

1. 이상진단 전용 딥러닝 모델과 고전 기법을 동일 조건에서 비교한다.
2. **point-adjust F1의 부풀림을 직접 재현한다.**
3. VUS-PR로 재평가하고 차이를 설명한다.

## 배경 개념

### 이 분야의 불편한 진실

TSB-AD 벤치마크는 40개 데이터셋·1,070개 시계열·40개 알고리즘을 평가했습니다. 결론은 이렇습니다.

> **단순한 아키텍처와 통계 기법이 종종 더 나은 성능을 낸다.** 신경망은 다변량에서, 파운데이션 모델은 point anomaly에서 유망하지만, "만능 해결책"이라는 약속은 아직 지켜지지 않았다.

그러므로 이 주는 최신 모델과 고전 기법을 **대등하게** 다룹니다.

### 대상 기법

| 계열        | 기법                      | 특징                                  |
| ----------- | ------------------------- | ------------------------------------- |
| 최신 딥러닝 | **CATCH** (ICLR'25) | 주파수 패칭 + 채널 인식 다변량        |
| 사전학습형  | **DADA**            | 다도메인 사전학습, 파인튜닝 없이 적용 |
| 고전        | **Sub-LOF**         | 서브시퀀스 국소 밀도                  |
| 고전        | **Matrix Profile**  | 서브시퀀스 간 최근접 거리             |
| 고전        | **IsolationForest** | 트리 기반 격리                        |
| 4주차       | LightGBM 잔차             | 여러분이 만든 것                      |

### point-adjust F1 함정 ⭐

이 분야에서 가장 널리 퍼진 평가 오류입니다.

**point-adjust란**: 이상 구간(예: 100개 시점) 중 **한 점이라도** 탐지하면, 그 구간 **전체를 맞힌 것으로 계산**하는 방식입니다.

왜 문제인가:

```
이상 구간 길이가 100인 데이터에서,
완전한 무작위 예측기가 1% 확률로 각 점을 이상이라 찍으면
→ 구간당 최소 1점을 맞힐 확률이 매우 높음
→ point-adjust F1이 0.9를 넘어감
```

**무작위 예측기가 F1 0.9를 냅니다.** 이 지표로 보고된 수많은 논문 성능이 이 효과에 오염되어 있습니다.

### VUS-PR

Volume Under the Surface of the PR curve. 임계값과 허용 오차 범위 둘 다에 대해 적분한 지표로, point-adjust의 부풀림에 훨씬 강건합니다. **TSB-AD가 표준 지표로 채택했습니다.**

## 실습 과제

**과제 9-1. TSB-AD 평가 모듈 도입**

- `TheDatumOrg/TSB-AD` 저장소에서 평가 모듈과 고전 기법 구현을 가져온다.
- LEAD 1.0 데이터를 이 모듈이 요구하는 형식으로 변환한다.

**과제 9-2. point-adjust 함정 재현** ⭐

- **무작위 예측기**를 만든다 (각 시점을 p 확률로 이상이라 찍음).
- p를 0.5%, 1%, 5%로 바꿔가며 **point-adjust F1**을 계산한다.
- 같은 예측기를 **VUS-PR**로도 평가한다.

| 예측기        | point-adjust F1 | VUS-PR |
| ------------- | --------------- | ------ |
| 무작위 p=0.5% |                 |        |
| 무작위 p=1%   |                 |        |
| 무작위 p=5%   |                 |        |

**이 표가 이 주의 가장 중요한 산출물입니다.**

**과제 9-3. 6종 기법 비교**

- 위 6개 기법을 LEAD 1.0에 적용하고, **point-adjust F1과 VUS-PR 둘 다로** 평가한다.
- 두 지표에서 순위가 다른가?

**과제 9-4. 이상 유형별 분석**

- 이상을 유형별로 나눈다 (점 이상 / 짧은 구간 / 긴 구간).
- 유형별로 어느 기법이 강한지 분석한다.

**과제 9-5. 이상 근거 제시**

- 탐지된 이상 3건에 대해, 해당 기법이 왜 그것을 이상으로 판정했는지 설명한다.
- 4주차 SHAP 설명문과 비교한다. **어느 쪽이 현장에서 더 유용한가?**

## 산출 기준

- [ ] TSB-AD 평가 모듈 연동
- [ ] **무작위 예측기의 point-adjust F1 vs VUS-PR 표** ⭐
- [ ] 6종 기법 비교표 (두 지표 병기, 순위 차이 명시)
- [ ] 이상 유형별 강약 분석
- [ ] 이상 근거 설명 3건 + 4주차 SHAP과의 비교
- [ ] 주간 보고서

## 내 결과가 정상인가 — 판정 기준

| 관찰                                                | 판정                                                      |
| --------------------------------------------------- | --------------------------------------------------------- |
| **무작위 예측기의 point-adjust F1 > 0.7**     | **정상이자 이 주의 핵심 결과입니다. 코드가 틀린 것이 아닙니다** — 디버깅하지 마십시오 |
| 같은 예측기의 VUS-PR이 이상 비율 수준(매우 낮음)    | 정상. 두 지표의 격차가 이 실험의 전부입니다               |
| 두 지표에서 기법 순위가 다름                        | **정상이자 기대 결과**                              |
| 고전 기법이 최신 딥러닝을 이김                      | 정상. TSB-AD의 보고와 일치합니다                          |
| 최신 모델이 특정 이상 유형에서만 우세               | 정상. 과제 9-4가 이것을 확인하는 과제입니다               |
| **CATCH/DADA가 환경 문제로 안 돌아감**        | 흔한 일입니다. **30분 이상 막히면 넘어가고, 실패 원인을 보고서 6번에 기록**하십시오. 포기 시점을 정하는 것도 역량입니다 |

## 자가 점검 질문

1. 무작위 예측기의 point-adjust F1은 얼마였는가? 이 숫자를 보고 무엇을 느꼈는가?
2. 최신 딥러닝 모델이 고전 기법을 이겼는가? 어느 지표로 봤을 때인가?
3. 두 지표에서 순위가 다르다면, 논문에서 point-adjust F1만 보고한 모델을 어떻게 봐야 하는가?
4. 현장에 배포한다면 어느 기법을 고르겠는가? 정확도 외에 무엇을 고려했는가?

---

# 10주차. 멀티에이전트 ① — 모델 라우팅

## 학습목표

1. 상황에 따라 예측기를 선택하는 라우팅 에이전트를 구성한다.
2. 라우터의 결정을 로깅하고 근거를 추적한다.
3. **최고 성능 단일 모델과 정직하게 비교한다.**

## 배경 개념

### 왜 라우팅인가

9주간의 실험으로 여러분은 이미 알고 있습니다.

- 이력이 짧으면 TSFM zero-shot이 유리하다
- 이력이 길고 공변량이 많으면 LightGBM이 유리하다
- 어떤 건물은 딥러닝이 낫고 어떤 건물은 아니다

**단일 모델이 모든 상황에서 최적인 경우는 없습니다.** 그렇다면 상황을 보고 고르는 계층을 하나 얹는 것이 자연스럽습니다.

### 라우팅 판단 기준

| 신호                          | 어느 쪽으로                         |
| ----------------------------- | ----------------------------------- |
| 이력 길이 < 3개월             | TSFM zero-shot                      |
| 이력 길이 > 1년 + 공변량 다수 | LightGBM                            |
| 강한 주기성 + 안정적          | Seasonal Naive로도 충분 (비용 절감) |
| 계열 수 많음 + 지연 요구 엄격 | TTM                                 |
| 최근 잔차 급증                | 이상 의심 → 이상진단 경로로 분기   |

### 규칙 기반 vs LLM 기반

|           | 규칙 기반        | LLM 기반                    |
| --------- | ---------------- | --------------------------- |
| 결정 방식 | if-else          | LLM이 상황 요약을 읽고 판단 |
| 재현성    | 완전             | 낮음 (온도 설정에 좌우)     |
| 설명      | 규칙 자체가 설명 | 자연어 설명 생성            |
| 비용      | 0                | API 호출당 과금             |

**본 과정은 규칙 기반을 기본으로 하고, LLM은 설명 생성(11주차)에 씁니다.** 이유는 두 가지입니다. 예측기 선택은 정량 신호로 충분히 결정 가능하고, 재현성이 필요한 곳에 비결정적 요소를 넣을 이유가 없습니다.

**단, LLM 라우터도 만들어 비교합니다.** 어느 쪽이 나은지가 이 주의 실험 대상입니다.

### LangGraph 구조

```
        [입력: 건물 ID, 예측 시점]
                  │
          ┌───────▼────────┐
          │  상태 수집 노드  │  이력 길이, 결측률, 공변량 유무,
          └───────┬────────┘  최근 잔차, 주기성 강도
                  │
          ┌───────▼────────┐
          │   라우터 노드    │  → 어느 예측기를 쓸지 결정
          └───────┬────────┘
      ┌───────────┼───────────┬──────────┐
      ▼           ▼           ▼          ▼
  [Naive]    [LightGBM]  [Chronos-2]  [TTM]
      └───────────┴───────────┴──────────┘
                  │
          ┌───────▼────────┐
          │   결과 수집 노드 │  예측값 + 선택 근거 로그
          └────────────────┘
```

## 실습 과제

**과제 10-1. 예측기 인터페이스 통일**

- 지금까지 만든 모든 예측기를 **동일한 인터페이스**로 감싼다.
- 라우터가 어느 것이든 같은 방식으로 호출할 수 있어야 한다.

**과제 10-2. 상태 수집 노드**

- 건물별로 라우팅 신호를 계산하는 노드를 만든다.
- 이력 길이 / 결측률 / 공변량 유무 / 주기성 강도 / 최근 잔차 수준

**과제 10-3. 규칙 기반 라우터**

- 위 표를 참고하되, **여러분의 1~9주차 실험 결과를 근거로 규칙을 직접 정한다.**
- 규칙마다 "왜 이 규칙인가"를 실험 결과로 뒷받침한다.

**과제 10-4. LLM 기반 라우터**

- 상태를 자연어로 요약해 LLM에 주고 예측기를 고르게 한다.
- **출력을 구조화**해서 파싱 가능하게 한다 (자유 텍스트 금지).

**과제 10-5. 정직한 비교** ⭐

- 같은 하네스에서 다음을 비교한다.

| 방식                                       | MASE(24h) | MASE(168h) | 평균 지연 | LLM 비용 |
| ------------------------------------------ | --------- | ---------- | --------- | -------- |
| 최고 성능 단일 모델                        |           |            |           | —       |
| 규칙 기반 라우터                           |           |            |           | —       |
| LLM 기반 라우터                            |           |            |           |          |
| **오라클 라우터** (사후에 최적 선택) |           |            |           | —       |

**오라클은 도달 불가능한 상한선입니다.** 라우터가 오라클에 얼마나 가까운지가 라우팅의 여지를 보여줍니다.

**과제 10-6. 실패 분석**

- 라우터가 잘못 고른 사례를 3건 찾아 원인을 분석한다.
- **라우터가 단일 모델보다 나빴다면, 그 원인 규명이 이 주의 산출물입니다.**

## 산출 기준

- [ ] 통일된 예측기 인터페이스
- [ ] 상태 수집 노드 (신호 5종)
- [ ] 규칙 기반 라우터 + **각 규칙의 실험적 근거**
- [ ] LLM 기반 라우터 (구조화 출력)
- [ ] **4행 비교표 (오라클 포함)**
- [ ] 라우팅 결정 로그 (전체 건물·윈도우)
- [ ] 실패 사례 3건 분석
- [ ] 주간 보고서

## 내 결과가 정상인가 — 판정 기준

| 관찰                                          | 판정                                                        |
| --------------------------------------------- | ----------------------------------------------------------- |
| 오라클이 모든 단일 모델보다 우수              | 정상 (정의상 그렇습니다)                                    |
| 라우터 점수가 오라클과 단일 최고 모델 사이    | 정상이자 성공                                               |
| **라우터가 단일 최고 모델보다 나쁨**    | **흔합니다.** 원인 규명이 산출물입니다 — ① 라우팅 신호가 실제 승패와 상관이 낮다 ② 규칙 임계값이 부적절 ③ 애초에 모델 간 차이가 작아 고를 여지가 없다. **③이면 "이 데이터에 라우팅은 불필요"가 정당한 결론입니다** |
| LLM 라우터 ≈ 규칙 라우터                     | 정상                                                        |
| **LLM 라우터가 규칙 라우터보다 나쁨**   | 예상되는 결과입니다. 예측기 선택은 정량 신호로 결정 가능한 문제라 LLM의 이점이 없습니다 |
| **LLM 라우터가 같은 입력에 다른 답**    | temperature가 0인지 확인. 0인데도 흔들리면 **그 사실 자체를 기록**하십시오 |

## 자가 점검 질문

1. 라우터가 최고 단일 모델을 이겼는가? 못 이겼다면 원인은 라우팅 자체인가 규칙인가?
2. 오라클과의 격차는 얼마인가? 그 격차는 좁힐 수 있는 것인가?
3. LLM 라우터는 규칙 라우터보다 나았는가? 비용과 비결정성을 감안하면?
4. 라우터의 결정 로그를 보고 "왜 이 건물에 이 모델을 썼는지" 설명할 수 있는가?

---

# 11주차. 멀티에이전트 ② — 오차 보정과 근거 설명 생성

## 학습목표

1. 이벤트 상황의 예측 오차를 보정하는 로직을 만든다.
2. 예측 근거를 설명하는 리포트를 자동 생성한다.
3. **생성된 설명의 사실성을 검증한다.**

> **이 주가 공고 주제 「예측 근거 설명 시스템」의 본론입니다.**

## 배경 개념

### 왜 설명이 필요한가

예측값만으로는 현장이 움직이지 않습니다. 설비 담당자가 듣고 싶은 것은 이렇습니다.

> "내일 오후 2시에 소비가 평소보다 30% 높을 것으로 예측됩니다. **이유는 외기온이 34도까지 오르기 때문이고**, 작년 같은 조건에서도 비슷한 패턴이 있었습니다. 다만 이번에는 냉동기 2호기의 최근 효율이 떨어진 상태라 예측보다 더 높을 수 있습니다."

여기에는 세 종류의 정보가 결합되어 있습니다.

| 정보      | 어디서 오는가  |
| --------- | -------------- |
| 예측값    | 10주차 라우터  |
| 기여 요인 | 4주차 SHAP     |
| 이상 상태 | 9주차 이상진단 |

### 오차 보정 (Error Correction)

이벤트 상황(폭염, 공휴일, 행사)에서 모델은 체계적으로 틀립니다. 이 체계적 편향을 보정하는 방법입니다.

| 방식            | 설명                               |
| --------------- | ---------------------------------- |
| 잔차 모델       | 잔차 자체를 예측하는 2차 모델 학습 |
| 상황별 오프셋   | 이벤트 유형별 평균 잔차를 더함     |
| 조건부 스케일링 | 조건(외기온 구간)별 배율 적용      |

**주의**: 보정은 과적합하기 쉽습니다. 반드시 **보정 파라미터도 학습 구간에서만** 추정하십시오. 이것을 어기면 2주차에 배운 누수를 다시 저지르는 것입니다.

### 설명 생성의 함정 — 환각 ⭐

LLM에게 "이 예측을 설명해줘"라고 하면 **그럴듯하지만 근거 없는 설명을 만들어냅니다.**

```
❌ 잘못된 방식
   LLM에게 시계열과 예측값만 주고 "설명해줘"
   → "여름철 냉방 수요 증가로 보입니다" (실제로는 겨울 데이터)

✅ 올바른 방식
   1. SHAP 값·공변량 실측치·이상 탐지 결과를 구조화된 형태로 계산
   2. 그 수치들을 LLM에 명시적으로 전달
   3. "제공된 수치만 사용하고, 없는 정보는 언급하지 말 것" 지시
   4. 생성된 문장의 모든 수치를 원본과 대조 검증
```

**4번이 필수입니다.** 검증 없는 설명 시스템은 현장에서 오히려 위험합니다.

## 실습 과제

**과제 11-1. 이벤트 구간 식별**

- 예측 오차가 큰 구간을 찾고, 원인별로 분류한다.
- 분류 기준 예: 극한 기온 / 공휴일 / 원인 불명

**과제 11-2. 오차 보정 로직**

- 위 3가지 보정 방식 중 하나 이상을 구현한다.
- **보정 전후 성능을 하네스로 비교한다.**
- 보정 파라미터가 학습 구간에서만 추정되었음을 검증한다.

**과제 11-3. 근거 데이터 구조화**

- 예측 1건에 대해 다음을 구조화된 dict/JSON으로 모은다.

```
{
  "prediction": {"value": ..., "interval": [...], "model_used": "...", "route_reason": "..."},
  "shap_top_features": [{"feature": "airTemperature", "value": 34.2, "contribution": 45.3}, ...],
  "covariates": {"airTemperature_forecast": 34.2, "is_holiday": false},
  "anomaly_status": {"is_anomaly": false, "recent_residual_level": "elevated"},
  "historical_analogue": {"date": "...", "similarity": 0.91}
}
```

**과제 11-4. 설명문 생성**

- 위 구조를 LLM에 전달해 자연어 리포트를 생성한다.
- 프롬프트에 **"제공된 수치만 사용"** 제약을 명시한다.

**과제 11-5. 사실성 검증** ⭐

- 생성된 문장에서 모든 수치를 추출해 원본과 대조하는 **자동 검증기**를 만든다.
- 이벤트 **3건**에 대해 리포트를 생성하고 검증 결과를 기록한다.

| 항목                   | 이벤트 1 | 이벤트 2 | 이벤트 3 |
| ---------------------- | -------- | -------- | -------- |
| 생성 문장 내 수치 개수 |          |          |          |
| 원본과 일치            |          |          |          |
| 불일치                 |          |          |          |
| 근거 없는 주장         |          |          |          |

**과제 11-6. 프롬프트 개선 루프**

- 검증에서 실패한 항목을 근거로 프롬프트를 개선하고 재측정한다.
- **개선 전후 불일치율을 비교한다.**

## 산출 기준

- [ ] 이벤트 구간 식별 및 분류
- [ ] 오차 보정 로직 + 보정 전후 성능 비교 + 누수 없음 검증
- [ ] 근거 데이터 구조화 스키마
- [ ] 설명문 생성 파이프라인
- [ ] **자동 사실성 검증기 + 이벤트 3건 검증 표** ⭐
- [ ] 프롬프트 개선 전후 불일치율 비교
- [ ] 주간 보고서

## 내 결과가 정상인가 — 판정 기준

| 관찰                                    | 판정                                                          |
| --------------------------------------- | ------------------------------------------------------------- |
| 첫 프롬프트에서 수치 불일치 발생        | 정상. 거의 항상 발생합니다                                    |
| 프롬프트 개선 후 불일치율 감소          | 정상이자 기대 결과                                            |
| **불일치율이 0에 도달하지 않음**  | **정상입니다. 0이 목표가 아닙니다.** 현 기술의 한계로 보고서에 기록하십시오 |
| 보정으로 이벤트 구간 오차가 개선        | 정상                                                          |
| **보정으로 전체 성능이 크게 개선** | **과적합·누수를 의심하십시오.** 보정 파라미터를 어느 구간에서 추정했는지 즉시 확인 |

## 자가 점검 질문

1. 보정이 성능을 개선했는가? 개선했다면 그것이 누수가 아님을 어떻게 확신하는가?
2. 첫 프롬프트에서 불일치율은 몇 %였는가? 개선 후에는?
3. 설명문을 설비 담당자에게 보여줬다고 상상해 보라. 그는 무엇을 하러 갈 것인가?
4. 이 시스템을 실제 배포한다면, 검증기가 불일치를 발견했을 때 어떻게 동작해야 하는가?

---

# 12주차. 최종 통합 및 발표

## 학습목표

1. 전체 파이프라인을 하나로 통합한다.
2. 12주 누적 비교표를 완성하고 해석한다.
3. 최종 보고서와 발표를 완성한다.

## 실습 과제

**과제 12-1. 파이프라인 통합**

```
[데이터 입력]
     │
[상태 수집] ──→ [라우터] ──→ [예측기 선택·실행]
     │                              │
[이상진단] ────────────────────────┤
     │                              │
     └──→ [근거 구조화] ──→ [설명 생성] ──→ [검증]
                                         │
                                   [최종 리포트]
```

- 위 흐름을 하나의 실행 가능한 파이프라인으로 만든다.
- **단일 진입점**에서 건물 ID와 시점을 넣으면 리포트가 나와야 한다.

**과제 12-2. Streamlit 대시보드**

- 최소 구성:
  - 건물 선택 → 예측 그래프 (실측·예측·예측구간)
  - 어느 모델이 선택되었는지와 그 이유
  - 이상 탐지 표시
  - 근거 설명문
- **화려함보다 정보의 정확성이 평가 대상입니다.**

**과제 12-3. 12주 누적 비교표 완성** ⭐

| 모델/방식               | MASE 24h | MASE 168h | VUS-PR | 추론 ms | 학습 시간 | GPU 필요 | 설명 가능 | 콜드스타트 |
| ----------------------- | -------- | --------- | ------ | ------- | --------- | -------- | --------- | ---------- |
| Seasonal Naive          |          |           |        |         |           |          |           |            |
| LightGBM (global)       |          |           |        |         |           |          |           |            |
| LightGBM + SHAP         |          |           |        |         |           |          |           |            |
| Chronos-2 zero-shot     |          |           |        |         |           |          |           |            |
| Chronos-2 + 공변량      |          |           |        |         |           |          |           |            |
| Chronos-2 + LoRA        |          |           |        |         |           |          |           |            |
| TTM                     |          |           |        |         |           |          |           |            |
| PatchTST                |          |           |        |         |           |          |           |            |
| iTransformer            |          |           |        |         |           |          |           |            |
| TimeXer                 |          |           |        |         |           |          |           |            |
| DLinear                 |          |           |        |         |           |          |           |            |
| CATCH                   | —       | —        |        |         |           |          |           |            |
| DADA                    | —       | —        |        |         |           |          |           |            |
| Sub-LOF                 | —       | —        |        |         |           |          |           |            |
| Matrix Profile          | —       | —        |        |         |           |          |           |            |
| **라우터 (최종)** |          |           |        |         |           |          |           |            |

**과제 12-4. 최종 보고서**

구성:

1. 문제 정의와 범위
2. 데이터·평가 설계 (2주차 하네스)
3. 모델별 결과 (계열별 정리)
4. **누적 비교표와 해석**
5. **네 가지 함정 재현 결과** — 누수 / 사전학습 오염 / look-back 민감도 / point-adjust 부풀림
6. 라우팅·설명 시스템 설계와 성능
7. **현장 조건별 권고** — 어떤 조건에서 무엇을 쓸 것인가
8. 한계와 향후 과제

**5번이 이 보고서를 다른 학생 프로젝트와 구분 짓는 부분입니다.** 반드시 실측 수치를 포함하십시오.

**과제 12-5. 최종 발표 (30분 + 질의 15분)**

## 산출 기준

- [ ] 통합 파이프라인 (단일 진입점 동작)
- [ ] Streamlit 대시보드
- [ ] **12주 누적 비교표 완성**
- [ ] 최종 보고서 — **이번 주 주간 보고서를 대체**
- [ ] 최종 발표 자료 및 수행
- [ ] 코드 저장소 정리 (README에 재현 절차 명시)

## 제출 전 자가 점검 — 최종 평가 체크리스트

> 이 항목들이 곧 평가 기준입니다. 제출 전에 스스로 대조하십시오.

- [ ] 12주 누적 비교표가 완성되어 있는가 (빈칸이 있다면 **왜 비었는지 각주로 기록** — 실행 실패·시간 부족·해당 없음은 각각 다른 정보다)
- [ ] **네 가지 함정 재현 결과가 실측 수치와 함께 제시되는가** (누수 / 사전학습 오염 / look-back 민감도 / point-adjust 부풀림)
- [ ] 결론이 조건부이고, **그 조건을 정확히 말하는가** ("축에 따라 다르다"가 정답이다. 정확도 1위·속도 1위·종합 추천이 각각 달라도 된다)
- [ ] 라우팅·설명 시스템이 실제로 동작하는가 (시연 가능한가)
- [ ] 설명 검증 결과(불일치율)가 보고되는가
- [ ] 한계를 정직하게 인정하는가
- [ ] 코드 저장소만으로 재현 가능한가 (README에 데이터 경로·환경·실행 순서)

## 자가 점검 질문

1. 누적 비교표에서 종합 1위는 무엇인가? 축을 바꾸면 1위도 바뀌는가?
2. 12주 전의 나에게 한 가지만 말해줄 수 있다면 무엇을 말하겠는가?
3. 네 가지 함정 중 실무에서 가장 위험한 것은 무엇이라고 생각하는가?
4. 이 시스템을 실제 건물에 배포하려면 무엇이 더 필요한가?

---

# 부록 A. 주간 보고서 양식

## A.1 제출 규칙

| 항목   | 규칙                                                                     |
| ------ | ------------------------------------------------------------------------ |
| 주기   | 주간 보고서**10회** (1~5, 7~11주차) + 종합 보고서 2회 (6·12주차) |
| 기한   | 매주**금요일 18시**                                                |
| 형식   | Markdown                                                                 |
| 파일명 | `주차별보고서_{주차2자리}_{이름}.md`                                   |

**6주차와 12주차에는 주간 보고서를 제출하지 않습니다.** 종합 보고서가 대체합니다.

## A.2 템플릿

```markdown
# 주간 보고서 — {N}주차

## 1. 기본 정보
- 주차 / 기간: {N}주차, YYYY-MM-DD ~ YYYY-MM-DD
- 작성자:
- 실습시간: 계획 8h / 실제 __h

## 2. 이번 주 목표
> 지난주 보고서 7번을 그대로 복사할 것.
> 목표를 변경했다면 변경 사유를 명기할 것.

## 3. 수행 내역
| # | 실험/작업 | 코드 위치 (커밋 해시 또는 파일명) |
|---|---|---|
| 1 | | |
| 2 | | |

## 4. 정량 결과   ★필수
| 모델/방식 | 데이터 | 지표 | 값 | 베이스라인 대비 |
|---|---|---|---|---|
| | | | | +__% / -__% |

> 베이스라인 = 2주차에 확정한 Seasonal Naive 점수

## 5. 해석
> 4번의 숫자를 인용하며 인과를 설명할 것.
> "성능이 좋아졌습니다"가 아니라
> "MASE가 0.82→0.71로 13% 개선되었는데, 이는 외기온 lag 피처 추가 때문으로
>  보인다. 피처 중요도에서 airTemperature_lag24가 3위로 진입했다."

## 6. 막힌 점   ★필수 — 4단 구조
| 문제 | 가설 | 검증 방법 | 결과 |
|---|---|---|---|
| | | | |

> "안 됐습니다"만 쓰면 반려됩니다.
> 해결하지 못한 것도 괜찮습니다. 무엇을 시도했는지가 평가 대상입니다.

## 7. 다음 주 목표
> 검증 가능한 형태로 쓸 것.
> ❌ "TSFM을 공부하겠다"
> ⭕ "Chronos-2를 하네스에 통합하고 LightGBM과 MASE를 비교한다"

## 8. 질문
> 기업 지도자에게 묻고 싶은 것
```

## A.3 채점 루브릭 (주당 20점)

| 항목             | 4점                                | 2점                          | 0점               |
| ---------------- | ---------------------------------- | ---------------------------- | ----------------- |
| 목표 달성도      | 2번 목표를 모두 달성               | 일부 달성, 사유 명확         | 미달성·사유 불명 |
| 정량성           | 4번 표에 베이스라인 대비 증감 포함 | 수치는 있으나 비교 기준 없음 | 수치 없음         |
| 해석 깊이        | 5번이 숫자를 인용하며 인과 설명    | 현상 서술에 그침             | 해석 없음         |
| 문제해결 과정    | 6번이 4단 구조를 갖춤              | 문제만 서술                  | 미작성            |
| 다음 목표 구체성 | 7번이 검증 가능한 형태             | 방향만 제시                  | "공부하겠다" 수준 |

## A.4 왜 이 양식인가

**4번과 6번이 핵심입니다.**

- **4번**은 "무언가 했다"를 "무엇이 얼마나 나아졌다"로 바꿉니다. 베이스라인 대비 증감을 항상 요구하므로, 2주차에 확정한 Seasonal Naive 점수가 12주 내내 기준점으로 작동합니다.
- **6번**은 막힘을 감추지 않고 **디버깅 과정 자체를 산출물로** 만듭니다. 실무에서 여러분의 가치는 "막히지 않는 것"이 아니라 "막혔을 때 체계적으로 뚫는 것"입니다.

**2번의 "지난주 목표 복사" 규칙**은 목표 표류를 드러냅니다. 목표가 매주 조용히 바뀌면 진도 파악이 불가능하므로, 변경을 금지하는 대신 반드시 기록하게 합니다.

---

# 부록 B. 평가지표 치트시트

## B.1 예측 지표

| 지표           | 수식                     | 범위 | 특성                                         | 언제 쓰나                                             |
| -------------- | ------------------------ | ---- | -------------------------------------------- | ----------------------------------------------------- |
| **MAE**  | `mean(\|y - ŷ\|)`       | 0~∞ | 큰 오차에 관대. 스케일 의존                  | 단일 계열, 오차 크기가 선형적으로 중요                |
| **RMSE** | `sqrt(mean((y-ŷ)²))` | 0~∞ | **큰 오차에 민감**. 스케일 의존        | 피크 오차가 치명적일 때                               |
| **MAPE** | `mean(\|y-ŷ\|/\|y\|)`     | 0~∞ | **y가 0에 가까우면 폭발**              | 본 과정에서는 권장하지 않음                           |
| **MASE** | `MAE / MAE_naive`      | 0~∞ | **스케일 무관**. <1이면 Naive보다 나음 | **여러 계열을 묶어 평가할 때 (본 과정 주지표)** |
| **wQL**  | 분위수 손실의 가중합     | 0~∞ | **확률예측 평가**                      | 예측구간이 필요할 때                                  |

### 스케일 문제 — 반드시 기억할 것

건물마다 소비 규모가 수십 배 차이 납니다.

```
❌ 건물별 RMSE를 그냥 평균
   → 가장 큰 건물 하나가 전체를 지배

⭕ MASE를 쓰거나, 건물별로 정규화한 뒤 평균
```

### 지표 안정성 순위

여러 윈도우에 걸쳐 순위가 흔들리지 않는 정도: **wQL > RMSE > MAE**

## B.2 이상진단 지표

| 지표                      | 특성                                | 주의                                                    |
| ------------------------- | ----------------------------------- | ------------------------------------------------------- |
| Precision / Recall / F1   | 점 단위 평가                        | 임계값에 좌우                                           |
| **point-adjust F1** | 구간 중 1점만 맞혀도 전체 정답 처리 | **⚠️ 무작위 예측기도 0.9 넘김. 단독 사용 금지** |
| AUC-ROC                   | 임계값 무관                         | **이상 비율이 낮으면 과대평가**                   |
| AUC-PR                    | 임계값 무관, 불균형에 강건          | 구간 이상은 반영 못 함                                  |
| **VUS-PR**          | 임계값 + 허용오차 둘 다 적분        | **본 과정 주지표.** TSB-AD 표준                   |

**보고 원칙**: 이상진단 성능은 **VUS-PR을 주지표로** 하고, point-adjust F1을 함께 보고할 때는 반드시 무작위 베이스라인 값을 병기하십시오.

## B.3 지표 선택 흐름

```
계열이 여러 개인가?
├─ 예 → 스케일이 다른가?
│        ├─ 예 → MASE
│        └─ 아니오 → RMSE 또는 MAE
└─ 아니오 → 피크 오차가 치명적인가?
             ├─ 예 → RMSE
             └─ 아니오 → MAE

예측구간이 필요한가? → wQL 추가
이상진단인가? → VUS-PR
```

---

# 부록 C. 트러블슈팅

## C.0 환경·설치 (1단계)

**Q. `ModuleNotFoundError: No module named 'chronos'` 가 납니다.**
A. Chronos-2는 `transformers`가 아니라 `chronos-forecasting` 패키지로 로드합니다. 교재와 같은 폴더의 `requirements_stage1.txt`를 그대로 설치했는지 확인하십시오. **개별 `pip install`로 채우지 마십시오** — 핀이 깨지면 6주차 TTM이 죽습니다.

**Q. TTM 로드에서 `AttributeError: ... has no attribute 'all_tied_weights_keys'` 가 납니다.**
A. `transformers` 버전이 `granite-tsfm`과 맞지 않습니다. **핀을 벗어난 상태입니다.** 가상환경을 지우고 같은 폴더의 `requirements_stage1.txt`로 다시 설치하십시오(`transformers==4.57.6`). 다른 패키지를 설치하다 `transformers`가 딸려 올라간 경우가 대부분입니다 — `pip show transformers`로 버전을 먼저 확인하십시오.

**Q. TTM에서 `Exception: Expecting freq_token in forward` 가 납니다.**
A. 오류가 아니라 **모델 선택의 결과**입니다. 6주차 과제 6-1의 함정 설명을 읽으십시오. 168h에서는 나지 않고 24h에서만 납니다. 그 이유를 찾는 것이 과제의 일부입니다.

**Q. `UnicodeDecodeError: 'cp949' codec can't decode byte ...`**
A. 한글 Windows에서 UTF-8 파일을 인코딩 지정 없이 열었습니다. **`open(path, encoding="utf-8")`** 로 여십시오. 저장할 때도 마찬가지입니다. 0.5절 작업 규칙 참조.

**Q. `huggingface_hub ... does not support symlinks` 경고가 뜹니다.**
A. Windows의 정상 동작입니다. 무시해도 되지만 캐시가 중복 저장됩니다. 디스크가 빠듯하면 개발자 모드를 켜거나 `HF_HOME`을 여유 있는 드라이브로 옮기십시오.

**Q. `torch`가 CUDA를 요구하거나 설치 용량이 수 GB입니다.**
A. Linux에서 기본 휠을 받은 경우입니다. `--index-url https://download.pytorch.org/whl/cpu` 를 붙여 다시 설치하십시오. Windows·macOS는 기본이 CPU 빌드입니다. `torch.version.cuda`가 `None`이면 정상입니다.

## C.1 데이터

**Q. `electricity.csv`를 읽으면 메모리가 부족합니다.**
A. 1주차에 선정한 10~20개 건물 열만 골라 읽으십시오. `pd.read_csv(..., usecols=[...])`. 전체를 읽을 이유가 없습니다.

**Q. 시간대(timezone)가 헷갈립니다.**
A. BDG2는 site별로 현지 시각 기준입니다. 기상 데이터와 결합할 때 site 단위로 맞추십시오. 서머타임 전환일에 중복·누락 시각이 생길 수 있으니 확인하십시오.

**Q. 결측이 너무 많은 건물이 섞여 들어갔습니다.**
A. 1주차 서브셋 선정을 다시 하십시오. **단, 후보 목록(`lead_labeled_candidates.csv`) 안에서만 고르고, 3주차 이후에는 바꾸지 마십시오.** 서브셋을 바꾸면 그 전 결과와 비교할 수 없습니다.

**Q. LEAD와 BDG2를 merge했는데 결과가 0행입니다.**
A. **dtype 불일치입니다.** `building_id_kaggle`은 결측 때문에 float(`0.0`), LEAD의 `building_id`는 int64입니다. pandas는 이 경우 오류 없이 빈 결과를 냅니다.

```python
meta = meta[meta.building_id_kaggle.notna()].copy()
meta["kaggle_id"] = meta.building_id_kaggle.astype(int)     # ← 형 맞추기
merged = meta.merge(lead, left_on="kaggle_id", right_on="building_id")
print("매칭된 건물:", merged.building_id_y.nunique())        # ← 반드시 확인
```

**Q. LEAD에 제 서브셋 건물이 없습니다.**
A. 1주차 후보 목록 안에서 골랐다면 **반드시 있습니다.** 위 dtype 문제를 먼저 확인하십시오. 그래도 없다면 후보 목록 밖에서 고른 것입니다. **이 경우에만 서브셋을 고쳐도 됩니다** — 목록 밖 건물을 목록 안 건물로 교체하고, **교체한 건물과 사유를 보고서에 기록한 뒤 1~3주차 결과를 재실행**하십시오. 그대로 두면 4주차를 수행할 수 없습니다.

**Q. 4주차만 데이터 기간이 짧습니다.**
A. 정상입니다. LEAD 라벨은 2016년 1년치뿐입니다. **4주차만 `n_windows=4`로 줄이고**(하네스 코드는 고치지 말 것), **조건이 1~3주차와 다르다는 사실을 보고서에 명시**하십시오.

**Q. `metadata.csv`를 읽었는데 3행짜리 이상한 표가 나옵니다.**
A. Git LFS 포인터 파일을 받은 것입니다. 파일 크기가 수백 바이트(약 130B)라면 실제 데이터가 아닙니다. `git lfs pull`을 쓰거나, 단일 파일은 **`media.githubusercontent.com/media/...`** 경로로 다시 받으십시오(0.4절). 지도자 사본이 있다면 그쪽이 확실합니다.

## C.2 평가

**Q. 백테스트 윈도우마다 점수 편차가 너무 큽니다.**
A. 정상입니다. 그것이 단일 홀드아웃이 위험한 이유입니다. 평균과 함께 **표준편차도 보고**하십시오.

**Q. MASE가 1보다 큽니다. 잘못한 건가요?**
A. Seasonal Naive보다 못하다는 뜻입니다. 흔한 원인: (1) horizon보다 짧은 lag을 썼다가 수정하면서 성능이 떨어짐 (원래가 누수였음), (2) 학습 기간이 너무 짧음, (3) 주기성이 강한 계열인데 주기 피처가 없음.

**Q. MASE가 0.3이 나왔습니다. 잘 만든 건가요?** ⭐
A. **거의 확실히 누수입니다.** 정상 범위는 0.6~0.9입니다. 좋은 결과가 나왔을 때 의심하는 것이 이 과정에서 배울 가장 중요한 습관입니다. 순서대로 확인하십시오.

1. **lag이 horizon보다 짧은가** — horizon 24h인데 `y(t-1)`이 피처에 있는가
2. **rolling 통계에 `shift(horizon)`을 걸었는가** — `y.rolling(24).mean()`은 현재 시점을 포함합니다. 가장 많이 놓치는 곳입니다
3. **MASE 분모를 테스트 구간으로 계산했는가** — 학습 구간의 Seasonal Naive MAE여야 합니다
4. **전처리 통계를 전체 구간에서 계산했는가** — 스케일러·결측 대체 전부 해당

넷 다 아니라면, 예측 시점 t의 입력에 t 이후 타임스탬프가 없는지 `assert`로 검증하십시오(아래 항목 참조).

**Q. Seasonal Naive의 MASE를 계산했더니 1.0이 아닙니다.**
A. **분모 정의가 틀렸습니다.** MASE의 분모는 **학습 구간**의 Seasonal Naive MAE입니다. 테스트 구간으로 계산하면 분모에 평가 대상 기간의 실제값이 들어가 그 자체가 누수이고, 값도 1.0에서 벗어납니다. 2주차 배경 개념의 경고 상자를 다시 보십시오.

**Q. 누수를 만들었는지 어떻게 확인하나요?**
A. 예측 시점 t에 대해, 모델 입력에 들어간 모든 값의 타임스탬프가 t보다 작은지 코드로 검증하는 assert를 넣으십시오. 사람 눈으로는 놓칩니다.

## C.3 모델

**Q. Chronos-2 API가 교재와 다릅니다.**
A. **모델 카드를 신뢰하십시오.** 라이브러리 버전에 따라 API가 바뀝니다. 교재의 코드는 개념 설명용입니다.

**Q. CPU에서 TSFM 추론이 너무 느립니다.**
A. **건물 수부터 줄이지 마십시오.** 참고 실측값은 노트북 4스레드 기준 Chronos-2가 건물 1개당 약 0.07초, 20건물 배치 1회 0.5초입니다. 이보다 크게 느리다면 자원이 아니라 구현 문제입니다. 순서대로 확인하십시오.

1. **윈도우마다 모델을 다시 로딩하고 있지 않은가** — 로딩만 20초 안팎입니다. 하네스 바깥에서 한 번만 로드하십시오
2. **컨텍스트를 매번 전체 시계열에서 복사하고 있지 않은가**
3. `torch.set_num_threads()`를 1로 잡아두지 않았는가
4. 그 다음에 배치 추론과 컨텍스트 길이 축소를 검토하십시오

건물 수를 줄였다면 **줄인 사실과 이유를 반드시 보고서에 기록**해야 다른 실험과 비교할 수 있습니다.

**Q. LightGBM이 이상하게 평평한 예측을 냅니다.**
A. lag 피처가 없거나 horizon 제약으로 모두 제거되었을 가능성이 큽니다. 피처 중요도를 확인하십시오. 캘린더 피처만 남으면 요일별 평균과 다름없는 예측이 나옵니다.

**Q. 딥러닝 학습이 주차 안에 안 끝납니다.**
A. **에폭 상한 10 · early stopping patience 3 · 서브셋은 1주차 그대로**를 지키십시오. 4모델 × look-back 3종 = 12회를 배정 시간 안에 끝내려면 이 상한이 필요합니다. 모든 것을 다 돌리는 것이 목적이 아니라 **동일 조건 비교**가 목적입니다.

## C.4 서버

**Q. GPU가 이미 사용 중입니다.**
A. `nvidia-smi`로 확인하고 상대에게 알리십시오. 순서를 정해 쓰고, 긴 학습은 시간을 나눠 잡으십시오.

**Q. 학습 중 CUDA out of memory가 납니다.**
A. 배치 크기를 절반으로 줄이십시오. 그래도 안 되면 look-back을 줄이거나 건물 수를 줄이십시오. **줄인 사실을 보고서에 기록**해야 다른 실험과 비교 가능합니다.

## C.5 에이전트

**Q. LLM이 매번 다른 예측기를 고릅니다.**
A. temperature를 0으로 두십시오. 그래도 흔들리면 그것이 LLM 라우터의 본질적 한계이며, 그 사실 자체를 보고서에 기록하는 것이 옳습니다.

**Q. 생성된 설명문에 없는 수치가 나옵니다.**
A. 환각입니다. (1) 프롬프트에 "제공된 수치만 사용" 제약을 강화하고, (2) **검증기를 반드시 통과시키십시오.** 검증 없는 설명 시스템은 배포하면 안 됩니다.

**Q. LLM API 비용이 걱정됩니다.**
A. 개발 중에는 이벤트 3건으로만 테스트하십시오. 전체 건물에 대한 설명 생성은 최종 시연 때 한 번만 돌리면 충분합니다.

---

## 마지막으로

이 과정에서 여러분이 얻어갈 가장 중요한 것은 특정 모델의 사용법이 아닙니다.

**"이 결과를 믿어도 되는가"를 스스로 판정하는 능력**입니다.

12주 동안 여러분은 누수가 만든 가짜 개선, 사전학습 오염이 만든 가짜 zero-shot, look-back이 만든 가짜 SOTA, point-adjust가 만든 가짜 F1을 **직접 재현해 봅니다.** 이 네 가지를 몸으로 겪고 나면, 앞으로 어떤 논문이나 벤더 자료를 봐도 무엇을 먼저 확인해야 할지 알게 됩니다.

그것이 기업이 신입에게 기대하는 능력이고, 이 과정이 존재하는 이유입니다.
